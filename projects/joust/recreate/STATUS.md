# Reconstruction status — Joust

Human-readable C reconstruction of Joust's 75 functions, each **verified byte-for-byte against
the original 68000 code** by the shared differential harness (`tools/recreate_kit`: a Musashi
oracle running the real code vs. the compiled reconstruction, on the same memory image). See
[`README.md`](README.md) for how this project binds to the kit, and
[`../../buggyboy/recreate/README.md`](../../buggyboy/recreate/README.md) for how the differential
method itself works.

**Verified: 75/75 — the table is complete.** Every function in `../decomp.c`'s inventory is
reconstructed and held against the original by the differential; `../names.txt` is the source of
truth for every name. The last row to land was `_start` itself, which closes the program: its
twenty-one calls and its loop's back edge are now executed, so nothing in the inventory is left
unreconstructed and nothing is left merely checkpointed.

**What "complete" does not mean.** Three walls remain, all of them the oracle's rather than the
reconstruction's, and each is recorded on the row it belongs to rather than summarised away: the
IKBD wait no run on either core can leave (`read_joysticks`, `title_screen`, `check_highscore`'s
entry loop and `_start`'s ninth call all stand on it); the traps whose whole effect is off-image
(Setpalette, Setcolor, Setscreen, Ikbdws, Kbdvbase — read back off the oracle's own stack, never
diffed); and the raw-floppy routine below, which is off the list entirely — now on its `Super` sites
alone, its PSG read having been unblocked by the kit's seeded read model. `_start` also carries the
one **fidelity** gap in the project — a register hand-off no C reconstruction can make — which its
row states in full.

## Method per function
1. Read the target in `../decomp.c` **and** the disassembly (`../out/joust_dis.txt`) — Ghidra
   renders Joust's register-argument routines as a bare `return;`, so the listing is not optional.
2. Write the idiomatic core in `src/<subsystem>.c` plus its glue `g_<name>` (the I/O contract).
3. Add address constants to `include/addrs.h`, prototypes to `include/joust.h`.
4. Add edge + fuzz cases in `test/test_<subsystem>.py`; `make test` must be green.
5. Mark the row below verified, with what the verification actually covered.

## Ordering
Leaf/pure functions first (no OS traps, simple contracts), then their callers, then the
trap-bound ones. The seven traps that used to block the sound, input and high-score-save layers —
`Super`, `Giaccess`, `Fcreate`, `Fwrite`, `Random`, `Bconstat`, `Bconin` — are modelled by the kit
now (`tools/recreate_kit/TRAP_MODEL.md`, pinned by `test/test_os_traps.py`). Two GEMDOS selectors
Joust also uses stay unmodelled on purpose and still raise: `Pterm` (0x4c) never returns, so there
is no post-state to diff, and `Dgetdrv` (0x19) asks about a machine the harness does not have.

## Off the list: the raw-floppy routine at `0x152dc`

**The PSG wall is gone; the `Super` wall is not.** It is still not one of the 75 rows below (it is
absent from `../decomp.c`'s inventory) and it is still unreconstructed — but the reason has changed,
so the remaining blocker is worth stating precisely rather than leaving the old blanket in place.

* **PSG read — UNBLOCKED.** Its drive-select subroutine (`0x15536`) selects PSG port A at `0x1553c`
  and reads it back at `0x15544` (`move.b $ff8800,d1`), then merges the drive bits into what it read.
  The kit used to reject **any** direct PSG read on its own, because the ledger recorded writes only
  and there was nothing correct to return. The kit's **seeded PSG read model** now serves it
  (`tools/recreate_kit/TRAP_MODEL.md`, Phase 6): a case declares the byte the chip held with
  `psg_seed={14: <byte>}` — an ordinary test input, given identically to both sides — and the
  read-back is answered from the declaration rather than from a fabricated `0`. The bits the routine
  *preserves* (port A bits 3–7, side select and the rest) are exactly what that declaration is for.
* **`Super` — STILL BLOCKED.** Its `Super` sites (Phase 2) stay unreachable while the model never
  leaves supervisor mode, and that is what keeps the routine off the list.
* **`Giaccess` alongside — still refused.** A run that reached both doors to the chip would trip the
  mixed-path guard, which Phase 6 does not retire: it gives the direct file readable contents, which
  is a different question from the two register files agreeing.

The old note said "it unblocks only when the oracle gains a real PSG read model". That model has
landed; what it bought is the drive-select read, not the enclosing routine.

## Functions (by address)

| Addr (Ghidra) | Name | Bytes | Status | Verification |
|---------------|------|-------|--------|--------------|
| `0x10000` | `_start` | 128 | ✅ verified | ALL TWENTY-ONE CALLS EXECUTED, and the loop's back edge with them, in TWO ROTATIONS around the one seam neither core can cross — the ninth call, whose IKBD wait ends on no run. ROTATION A (0x10000 -> the `jsr` at 0x10030) puts the four one-shot calls AND the frame's first four in ONE compare, over a console key that chooses a game: screen_base is XBIOS Physbase' answer rather than a poke (asserted through playfield_bottom), the title picture is copied out of a NOISE data segment onto a NOISE framebuffer, the chosen mode rides out to two_player_mode and the key is proved CONSUMED. It is also the only case anywhere that executes `title_screen` THE FUNCTION rather than its first-pass seam — a game key is the one input for which its `for (;;)` is left on the first pass. poison MEASURED safe. ROTATION B (0x10036 -> 0x10030) is ONE WHOLE LAP: calls 10..21, the `bra.s` at 0x1007e, and calls 5..8 again. Each rotation PAIRED with a never-returns proof at an ARGUED cap — asserted to exceed 4x the instructions the checkpointed run itself costs, MEASURED per run. THE LAP IS WALKED OVER THE GAME'S OWN FRAMES rather than the opening one: nine states, each the image the ORIGINAL played ITSELF into, stepped past the wait through read_joysticks' own verified rotated entry (0x11db0) with random sticks. Constructed staging is not an option here — render_object_body dereferences the destination it stored on the frame before, so noise is not a different case, it is a crash. THE DEEP LAPS ARE MEASUREMENTS AND ARE PINNED AS SUCH: three of the head's four calls sleep through the opening frames, so a shallow walk left them unheld (MEASURED — dropping raise_floor or update_eggs from the head SURVIVED that battery). raise_floor's counter is armed at frame 100 and lifts the lava surface at 106; the first egg is on the board at 159; test_the_deep_laps_are_where_the_sleeping_head_calls_wake asserts each, and asserts the floor's SURFACE MOVE separately from its cheap timer step, so a walk that stopped reaching an arm fails instead of quietly going shallow. THE COMPOSITION ACROSS THE SEAM IS HELD TOO, and by the one test here that is not a differential: the candidate is a shared library in this process and the IKBD wait is `volatile` because something outside the routine ends it, so a thread poking the image IS that interrupt — read_joysticks' technique, one level up. test_start_laps_its_frame_loop_when_the_ikbd_replies runs `start` ITSELF in a FORKED CHILD (the routine never returns, so a thread driving it would spin for the rest of the session and cost a core under `-n auto`; and a frame loop walking evolving state is the one place a wild pointer could take the pytest worker down with it), waits for read_joysticks' clear, asserts the frame's HEAD HAS RUN and its TAIL HAS NOT, dwells a quarter-second to prove the block, then plays the reply and watches the next lap arrive. IT CAUGHT A MUTANT BOTH ROTATIONS MISS — deleting frame_pass_head from the loop, which is invisible to them because each glue calls the head itself. The two marker bytes it reads laps off are pinned against the ORACLE'S OWN WRITE SETS for the very frames it walks, not asserted as prose. ORDER, AND THE CALLS WHOSE OWN EXITS NEVER RETURN, ARE PINNED AGAINST THE ORIGINAL'S ENCODINGS: all 21 `jsr <abs>.l` targets resolved through names.txt IN ORDER, and the `bra.s` at 0x1007e proved to close on the FIFTH call with its own end exactly 128 bytes from the entry. BOTH GLUES REFUSE, each with a positive pair at the bound: g_start turns away any console title_screen would not return from (9 stagings, including the CONSTRUCTED longwords 0x0131/0x0132/0x0103 that separate the key WIDTHS — the digits are `cmp.w`s, Ctrl-C a `cmp.b`), asking title_key_chooses_game, THE SAME TWO COMPARES the attract pass decides with rather than a second copy of them; g_start_frame_pass turns away a console key poll_quit_key would not come back from and a game whose leader has taken the record. Both questions are asked of the routine that owns them — poll_quit_key_comes_back and check_highscore_comes_back, each defined next to its routine and deciding through that routine's own constants and helpers — so the second is the EXACT gate rather than a blanket over game_over_flag, which is what lets a finished game whose record still stands run check_highscore for real and hold the call. Every candidate-side entry ALSO goes through a wall-clock deadline, so a deleted refusal is an ORDINARY RED rather than a hung worker. FOUR CALLS ARE WOKEN BY STAGING RATHER THAN BY PLAY, each with the SMALLEST POKE that decides its own first test and no more: the mixer and priority that make snd_poll_done release the chip; a finished game whose record still stands, which makes check_highscore pick a leader; wave_num at TROLL_FIRST_WAVE, which makes lava_troll tick its step timer; and GA_ROWS_LATCH at 1, which makes animate_ground_shrink step the burn timer. Each claims only the gate and the counter behind it — all four stop short of any table lookup, which is the line between staging an input and FABRICATING a record. 48/51 MUTATION SWEEP on the final tree, every mutant forced through a relink and the whole sweep re-run at the end. THREE SURVIVORS, and they are ONE CLASS with one sentence between them: on every frame the walk can reach, that call — or that order, or those registers — changes nothing, so no differential here can tell a reconstruction that made it from one that did not. Each is named below AND each has a test that MEASURES it, so the day a walked frame does separate one, the disclosure fails and asks for the case it can then have. THE TWO REFUSALS ARE DECIDED ON THE STAGED IMAGE, AND THE PASS CAN INVALIDATE THEM FROM INSIDE — draw_messages (call 15) can set game_over_flag and score_update moves the digits the record is compared against, so a lap that starts safe could reach call 21 unsafe. That is a BOUND on the refusal rather than a hole: such a run fails loudly on both sides (the oracle at its instruction cap, the candidate at the test's wall-clock deadline) instead of passing, and no frame of the walk reaches it. LIMITS: (1) THE STALE REGISTER HAND-OFF IS A FIDELITY GAP, NOT A HARNESS LIMIT — the only one in this reconstruction, and the first of the five survivors. update_objects reads D0 and D2 before writing them, and at 0x10036 both hold whatever read_joysticks' last control_player left behind. The rotated pass takes them as arguments and every lap is entered with a non-zero pair on BOTH sides, so the argument chain is executed rather than merely declared; but no frame in the walk makes them MATTER — D0 only reaches rng_advance's restart offset when the cursor wraps and D2 only the type-1/2 probe, so moving either register ON ITS OWN or both together gives the same image below the stack guard on every diffed frame — asserted there, and measured over a SIXTY-FRAME sweep besides, for the whole pass and for update_objects alone. A diagonal alone could not tell a frame where the two cancel from one where neither is read, which is why all three extremes are swept. AN EARLIER REVISION OF THIS ROW CLAIMED THE OPPOSITE — that the registers separated from the sixth frame on — and it was an artefact: the two bytes that moved were the ORACLE'S OWN SAVED REGISTERS, above the stack guard and outside the comparison, caught by a whole-image compare that should have stopped at it. update_objects' OWN battery is what holds the two registers. And `start` itself has no way to carry a register across a call, so it passes 0: AN ON-TARGET C BUILD DIVERGES HERE. Closing that means read_joysticks handing its callee's D0/D2 back, a contract change to two verified functions — left as an integrator's note rather than taken. (2) dissolve_platforms IS THE ONE CALL NOTHING HERE EXECUTES, the second survivor — and it is the only one of the three sleepers for which that is FORCED. lava_troll and animate_ground_shrink were disclosed here too until an adversarial review showed the disclosure was too broad: each has a SCALAR first observable effect (a step timer) reached before any table is indexed, so a one- or two-byte poke of the gate's own operand wakes it without fabricating anything, and both now have positive cases and dead mutants. dissolve_platforms has no such shape: its first act is to walk effect_table, so the earliest observable effect lies PAST a slot whose kind indexes a sprite pointer, and arming it means writing the effect RECORD the wave machinery writes — which CLAUDE.md forbids as a way to make a branch look covered. Its PRESENCE is pinned against the ORIGINAL'S `jsr` encoding alone; the ROUTINE is verified at its own entry (test_world.py), and test_the_one_call_no_frame_in_the_walk_can_hold asserts the reach on every walked frame. THE LESSON IS THE BROADER ONE: 'it cannot be armed without fabricating a record' has to be checked against where the routine's FIRST observable effect sits, not against where its interesting work does. (3) THE ORDER OF THE TAIL'S FIRST TWO CALLS is the third survivor: update_pterodactyl reads the active_players census update_objects rebuilds, so the order COULD show — but nothing in a frame changes which players are on the board between the two, and both orders agree over a 210-FRAME sweep — the same depth that found the head pair's one separating frame, so the two are comparable, and a 260-frame sweep agreed. The IN-SUITE assertion re-checks the nine walked frames only, so a separating frame outside them would not be noticed; it is a tripwire on the frames the suite already pays for, and the day it fires the answer is the head pair's — put that lap in the walk and make it an ordinary positive case. (4) NO POISON ON ANY LAP, and the reason is stronger than 'unsafe': inverting a frame's write set does not merely test a DIFFERENT case, it makes the ORACLE'S OWN re-run reach the PSG ports directly, which the model cannot serve — so there is no second case to compare against at all (test_poison_on_a_lap_would_not_even_be_a_valid_run). Rotation A does carry poison: from cold, the bytes it writes are ones it computed rather than ones it reads back. (5) FOUR CALLEES HAVE PATHS THE ORIGINAL NEVER COMES BACK FROM (title_screen's IKBD wait and its Ctrl-C, read_joysticks' restart, poll_quit_key's quit/restart/pause, check_highscore's entry loop) and the C's versions all return. `start` ignores every result exactly as the original's twenty-one `jsr`s do, which is CORRECT on target — the routine that took such a path does not come back, so the next line is never reached — but it means no case shows what a C `_start` does after one of them, and the two glue refusals are precisely what keep every case away from them |
| `0x10080` | `init_system` | 488 | ✅ verified | the 16-pen Setcolor read-back with a sentinel past pen 15 and the final cursor asserted (pins cmpi.w #$10 and the word step); Super(0); conterm saved WHOLE then andi.b #$f8 over 8 values; both KBDVBASE vectors saved and both handlers installed; Physbase -> screen_base; HIGH.SCO opened/read/closed, with Fread handed the handle STILL IN D0 while Fclose re-reads it from memory — both transcribed; a 10-call battery reading Getrez/Setcolor (FIRST and LAST pen)/Super/Kbdvbase/Setscreen/Physbase/Ikbdws/Fopen/Fread/Fclose arguments back out of the oracle at a stop_pc on each trap's return address — NOT off the write set, which records each address's FINAL value and so reports only a later trap's push; 36-case sharded fuzz; poison MEASURED safe. LIMIT: Getrez is modelled as a constant 0, so the MONOCHROME branch (0x10090..0x1013d) is unreachable and unverified — reproduced, with its ten constants pinned against the ORIGINAL'S instruction encodings instead. The Fopen-failure arms of both file loads are unreachable by construction |
| `0x102e2` | `fill_screen` | 28 | ✅ verified | 32008 bytes from screen_base — 8 PAST the framebuffer, reproduced; colour words incl. 0x100/0x101; poison |
| `0x102fe` | `make_fill_pattern` | 54 | ✅ verified | colour bytes 0..0xff exhaustively + long-D0 edges + 400-case fuzz; D1/D2 captured via a store stub, poison on 10 shapes |
| `0x10334` | `fill_pattern_n` | 10 | ✅ verified | counts 1/2/3/0xff/0x100/0x101/0x1000/0xfffe/0xffff x 4 patterns, count 0 = 65536 cells, hi-garbage counts, 400-case fuzz; end pointer == oracle A0 |
| `0x1033e` | `count_objects_and_pad` | 236 | ✅ verified | live/egg counts over all 8 slot shapes incl. the respawn+prev_dst rule, player-1 egg window, message-char walk + 0x23 cap, game_phase selection; poison on 5. The 3 pad loops touch no memory |
| `0x1042a` | `blit_copy` | 44 | ✅ verified | 18 shapes (row overlap, 0 = 256, low-byte-only counts) + 300-case fuzz, noise src/dst, poison on 5, plus a rectangle laid over its own argument block to pin the per-row `cols` re-read |
| `0x10456` | `blit_or` | 44 | ✅ verified | same battery as blit_copy |
| `0x10482` | `blit_andnot` | 48 | ✅ verified | same battery as blit_copy |
| `0x104b2` | `init_video` | 124 | ✅ verified | Setpalette's table pointer read back off the oracle's stack (the palette write is off-image); fill_screen, draw_platforms, score_update_p1/p2, draw_lives_p1/p2 and snd_tone_sweep all driven for real; the score bar checked against an INDEPENDENT model of its six offsets as well as the diff; 3 screen bases x 6 noise seeds x both life counts incl. the SIGNED 0x80/0xff rows; 12-case sharded fuzz; poison MEASURED safe. `moveq #$ff,d2` SIGN-EXTENDS, so the bar's planes-2/3 longword is 0xffffffff — the differential caught the byte reading with 72 diffs |
| `0x1052e` | `draw_platforms` | 148 | ✅ verified | signed present byte incl. the -1 latch, pairs 2/3 + 6/7 forced to the larger, the present POINTER indirection pinned with a permuted map, subq.b extents (0 = 256); poison on 3, 4 screen bases |
| `0x105c2` | `rng_advance` | 46 | ✅ verified | 12 edge cursors (wrap threshold, longword wrap, signed-negative) x 10 mixes with poison + hi-garbage mixes + 600-case fuzz; D0 preserved |
| `0x105f0` | `init_game` | 272 | ✅ verified | both template copies in the original's order with their SIGNED and equality bounds; the two OBJ_SCORE_PTR relocations at FULL longword width over 5 screen bases incl. one that wraps 32 bits; the four table sweeps asserted slot by slot with the bytes PAST each cleared field proved untouched — which is what pins stride and exclusive bound; the wholesale enemy-slot clear with the two player records proved NOT part of it; andi.l #$fe over 11 Random values (rng_ptr's reset is a RELOCATED immediate: 0 in the file, 0x10000 loaded); every scalar; 48-case sharded fuzz; poison MEASURED safe |
| `0x10700` | `draw_string` | 622 | ✅ verified | both fonts x all 236 non-control glyphs, 16 shifts/colours/bg, all 6 control bytes, backspace cell-borrow, text drawn onto its own state block (per-plane re-read), 200-case sharded fuzz |
| `0x1096e` | `snd_tone_sweep` | 232 | ✅ verified | 10 staged mixer values pinning the read-modify-write (bits 6-7 survive or.l #$3f + bclr #0/#1), whole register file staged to noise to pin exactly WHICH 8 registers it writes, both counters' residues (0xfffe/0xffff) pinning subq.w + SIGNED bge; poison. LIMITS: the intra-pass ORDER of the 7 register writes is INVISIBLE (Giaccess is a register file, not an ordered ledger — TRAP_MODEL.md Phase 3); shipped order checked by hand at 0x10990..0x10a3a. The volume start (0xf) is unobservable and the pitch start's MAGNITUDE among non-negative even values is too — both pinned against the ORIGINAL'S instruction encodings at 0x10980/0x10988 (opcode + immediate + target). The differential does pin the pitch start's parity and sign |
| `0x10a56` | `play_sound` | 52 | ✅ verified | signed cmp.w gate over 96 index x priority pairs incl. 0x7fff/0x8000/0xffff, the SIGN-EXTENDED word table index over 12 wrapping indices (0x4000 reads entry 0, bit 13 flips direction), a staged distinct-pointer table asserted through the kit's Dosound ledger, the game's own 16 entries, 300-case sharded fuzz; poison on the negative indices — the only ones where a poisoned priority still admits the sound. Cannot use abi.stack_call_pokes: its 60-byte movem save area would land in diffed memory, so a word-arg stub keeps all stack traffic inside the guard band (no exclude band needed) |
| `0x10a8a` | `snd_poll_done` | 36 | ✅ verified | ALL 256 mixer bytes (the andi.b #$3f masking of bits 6-7 is exhaustive, not sampled), 7 priorities incl. 0x7fff/0x8000/0xffff, release is unconditional; only register 7 is consulted, proven with the other 15 staged to noise; poison on both branches |
| `0x10aae` | `title_screen` | 408 | ✅ verified | ALL FOUR EXITS EXECUTED — instruction coverage 87/87 — one of them diffed at `rts`, two at checkpoints PAIRED with never-returns proofs, and one (the IKBD wait's own exit) honestly unverifiable. '1' AND '2' run to the `rts` at 0x10c44 and put the whole routine in ONE compare: fill_screen, the 0x7d00 title-picture copy, the three draw_string lines each in its own colour, cycle_palette + the six-pen ring, the Dosound silence, snd_poll_done and the play_sound gate on the kit's ledger, players_alive/two_player_mode, both players' units digit and lives — with the one-player arm's `clr.w` over player 2's flags proved NOT to happen on the two-player arm, and the two-player arm's player-2 record proved NOT written by the one-player one. That `clr.w`'s WIDTH needed a FOUR-byte sentinel: the base image is already zero in the two bytes above the flags, so a two-byte one could not tell it from a `clr.l` (measured — the wr32 mutant survived until the poke was widened). The player-record asserts read the ORACLE'S WRITE SET rather than its final image, so 'untouched' means the original never stored there at all. poison MEASURED safe. THE DESTINATION IS READ FROM screen_base, pinned by the one case that moves the base (every other stages the same one, so a baked-in address would agree with all of them), including the eight bytes fill_screen paints past the framebuffer following it. THE PICTURE: load_buffer is staged as NOISE, so a copy from any other address is a diff, and the checkpoint at 0x10ad0 (before any text is drawn over it) shows the framebuffer holding exactly those 0x7d00 bytes with the EIGHT bytes fill_screen paints PAST the framebuffer surviving — which is what pins the `cmpa.l` bound as exclusive. One case runs on the PRG's own placeholder artwork instead; every other picture byte in the battery is constructed. THE PALETTE RING is read off sixteen DISTINCT pens: the six walk one place round the closed cycle 10 -> 4 -> 6 -> 3 -> 8 -> 9 -> 10 and the other ten are asserted untouched, which is what says the ring is those six. ITS ORDER AGAINST cycle_palette IS HELD BY THE DIFF — unusually for this reconstruction — because pen 4 is IN the ring: the hue the cycle has just relocated is handed to pen 6 one instruction later, asserted BY VALUE (0x0300 -> 0x0003 with the counter one short of a carry). ONE ROTATION PER RUN IS ALL THERE IS: the loop is re-entered only through the IKBD wait, which no run leaves, so no case can rotate the ring twice. THE NO-KEY PATH IS DIFFED, not merely bounded: everything the attract pass writes is compared at a 0x10bb8 checkpoint, with ikbd_packet proved CLEARED over a poked sentinel and the poll counter proved SPENT to 0, PAIRED with a proof no uncapped run reaches `rts` — the reply that would end the spin arrives on an IKBD INTERRUPT the oracle never runs (TRAP_MODEL.md's limit 1). A key it does not act on ends the same way, with the console proved CONSUMED and both game globals still holding their sentinels. THE KEY WIDTHS are separated by CONSTRUCTED console longwords, and by nothing else: Ctrl-C is a `cmp.b` and '1'/'2' are `cmp.w`s, so 0x0131/0x0132 are ignored while 0x0103 quits — low words real TOS cannot produce (Bconin returns scancode << 16 \| ascii), labelled as constructed. CTRL-C, THE SEVENTH TRANSFER THAT IS NOT A CALL: `beq.w` at 0x10bea into 0x11c56, the MIDDLE of poll_quit_key, past that routine's entry and its own Bconstat/Bconin. It CANNOT be a call to poll_quit_key, so that block is exported from src/input.c as `quit_to_desktop` and shared by both callers; the run is diffed at the tail's own pre-Pterm checkpoint (0x11d4c) over test_input's staging, with BOTH Dosound silences on the ledger and hiscore_name's bytes proved to reach the staged HIGH.SCO, PAIRED with a never-returns proof over the SAME pokes at an ARGUED cap (asserted to exceed 4x the 103,951 instructions the checkpointed run itself costs). THE JOYSTICK START past the wait is verified at a ROTATED entry — the oracle entered AT 0x10bb8 with a reply staged, the same rotation hiscore_joystick_input uses and for the same reason (the prologue clears ikbd_packet) — over fire in either byte, both, and neither, x two_player_mode 0/1/0x80/0xff (a `tst.b`), with the no-fire branch back to 0x10b22 diffed clean at 0x10b22 and the ORACLE'S write set asserted empty below the stack guard — which says the ORIGINAL stores nothing on that path, the candidate being held to it by the diff — paired with its own never-returns proof. ITS GLUE REFUSES TWO PACKET POINTERS, for two different reasons, both pinned with a positive pair at the bound: an EMPTY one, because the reconstruction's spin is uncapped and would hang a pytest worker with no output; and one OUTSIDE THE IMAGE, because the routine dereferences it and the two cores disagree about what lies out there — the oracle's callbacks answer 0 for any address past its size, while the candidate would index host memory past the end of the buffer. Neither reading is the ORIGINAL'S, so a diff over one would prove nothing. The reconstruction itself stays unguarded and faithful. THE WAIT IS A `tst.l`, AND THREE OF ITS FOUR BYTES ARE PINNED BY STAGING. That took a second reply buffer: at 0x60000 the pointer's second byte is 0x06, so a wait reading only the first two bytes agrees on every case — the width looked like a harness limit and was a property of the chosen address. With the buffer at 0x00c000 a narrowed wait never terminates, and `_title_ikbd`'s wall-clock deadline turns non-termination into an ORDINARY RED (measured: narrowing to two bytes fails exactly those cases; so does deleting the glue's zero probe). The FOURTH byte — the pointer's most significant — cannot be reached by any legal pointer, since every address in a 0x100000-byte image has it zero; that one is an equivalent mutant under the model and is covered against the ORIGINAL'S encoding at 0x10bb8 instead. NOTHING here is verified only by a hang: the deadline is what makes the candidate's uncapped spin an assertable failure. 256-key sharded fuzz (every ASCII byte x a fresh picture, palette, counter, mixer and mode, each case stopped where its key allows) plus 120-case sharded joystick fuzz classified by the OR of the two bytes, so a corpus that stopped covering an arm fails instead of sharding cleanly. 34/40 mutation sweep, every mutant forced through a relink and the whole sweep re-run on the final tree. SIX survivors, each its own class and every one named below; NO mutant survives only by hanging. LIMITS: (1) THE ATTRACT LOOP'S RE-ENTRY is unpinned. g_title_screen stops after one pass, which is every reachable run, so nothing executes the second time round the `for (;;)`. It shares the painting-plus-first-pass seam with the routine rather than re-composing it, so a step dropped from EITHER fails a case (measured: deleting `draw_title_screen` from the shared seam fails at once, where deleting it from a glue-side copy would not) — what is left unheld is only the two calls the loop makes on a lap no run takes. (2) An unrecognised key branches back to the POLL HEAD rather than to the counter, costing the pass nothing — both runs end at the wait with the counter at 0, so the differential cannot tell them apart; pinned against the `bne.s` displacement at 0x10bf8 instead (measured survivor). (3) The poll's `bne` is a zero test, not a sign test, and from 400 the two agree — pinned against the opcode at 0x10ba0 (measured survivor). (4) Bconstat's answer is tested with `tst.b`/`blt` where poll_quit_key uses a full `cmp.l #-1`; the model answers only 0 or -1, so a plain non-zero test is an EQUIVALENT mutant (measured survivor). (5) Setpalette and Ikbdws change no memory, so the palette table, the IKBD command byte and its length word are read back off the ORACLE'S OWN STACK at checkpoints on each trap's return; the reconstruction issues no Ikbdws at all, exactly as init_system's does not, so an on-target build has to re-issue it or the joysticks are never interrogated. (6) AND THAT READ-BACK PINS THE ORIGINAL ONLY, WHICH COSTS TWO MORE SURVIVORS: xbios_setpalette has NO image effect and title_screen drops its return, so DELETING EITHER of its two calls (0x10aae's and 0x10b64's) leaves the whole battery green (measured, both). The same shape as the Ikbdws omission above, and it cannot be closed the way Dosound's is — that trap has a kit-wide ordered ledger both cores write, Setpalette has none, and giving it one is a kit change. So the two calls are pinned as the ORIGINAL'S `bsr` encodings and as the trap arguments it pushed, and the reconstruction's own two calls are honestly unheld. (7) THE 4 x 4 JOYSTICK BATTERY IS ZIPPED, not crossed: the fire test reads only the reply bytes and the mode test only two_player_mode, so the axes are independent — all four fire shapes and all four mode bytes appear, but no case pairs a given shape with more than one mode. The fuzz crosses them randomly instead |
| `0x10c46` | `xbios_setpalette` | 16 | ✅ verified | 16 bytes of nothing but the call, and its whole effect is off-image: the XBIOS selector and the table pointer are read back off the ORACLE'S OWN STACK at a stop_pc on the trap's return address (0x10c52), and the candidate's returned pointer compared against them — the same footing as init_video's Setpalette and flash_hiscore_color's Setcolor. The pointer is a RELOCATED longword (0xcd2 in the file, 0x10cd2 loaded), so a reconstruction holding the file's raw immediate fails. PAIRED with a measurement that the ORIGINAL writes NO image byte below the stack guard, which is the premise that lets the read-back stand alone — and why poison is vacuous here rather than skipped: there is no written byte to poison, and for the same reason the differential's own image compare cannot fail whatever the reconstruction does. The table's 16-pen extent pinned as title_palette + 0x20 == players_alive |
| `0x10c56` | `cycle_palette` | 124 | ✅ verified | all 8 values of the selector (counter bits 8-10), each stated as the colour word it produces rather than recomputed, so both nibble shifts are pinned BY VALUE; the zero-selector reset proved to STORE 0x100 over the whole counter rather than OR it in (0xf800 -> 0x0100), against cases where a selector IS present and the high bits survive (0x08ff -> 0x0900); addq.w's WORD width pinned by a sentinel in the next word; the blue > green > red hue priority over 5 pens a single component cannot state; every non-zero level of a component (15 x 3 source positions, into all three), which is what pins the masks as nibbles; bits 12-15 dropped by the rebuild; the colourless pen returning with the pen UNWRITTEN, high nibble intact; 400-case sharded fuzz over counter x pen x a fresh noise table, half the counters with the low byte SATURATED so the step CARRIES into the selector, which is the only way the selection changes at all — and test_cycle_palette_fuzz_carries_into_every_selector asserts over the whole stream that a carrying step reaches all 8 selectors incl. the reset, so deleting the saturation fails loudly instead of silently. Deliberately NOT phrased as "all 8 selectors occur": 400 uniform draws give that by themselves, so that assertion passes with the saturation removed and guards nothing (measured, then rewritten); the WHOLE 16-word table staged under noise, so a step aimed at the wrong pen shows as a diff. poison MEASURED safe (both outputs steer the next step, but the poisoned image is run through BOTH cores, so an inverted counter is a different and equally valid case) and carried on every hand-written row, with the green/red-source and wrap cases the exhaustive sweep runs unpoisoned covered explicitly. 21/21 mutation sweep (both routines, forced relink per mutation). WHAT THE WRITE-SET READS DO AND DO NOT PROVE: `_wrote` reads the ORACLE's writes, so every expected colour/counter above is a statement about the ORIGINAL — which is what stops a battery passing vacuously with both sides writing nothing, and the only way to tell a written-but-identical word from an untouched one. The RECONSTRUCTION is held to it by the image diff alone, so a version that wrote the pen back unchanged on the early-return path would be image-equivalent and green. LIMITS: the two extractions mask before they shift, so `lsr.w` and `asr.w` answer identically for every input and no staged pen can separate them — the unsigned `>>` is pinned against the ORIGINAL'S INSTRUCTION ENCODING (the shift-type field at 0x10c88/0x10c94) instead. And MOST OF THE STAGED PENS ARE SYNTHETIC, which the row says outright rather than letting the sweep look data-backed. The shipped title_palette holds 0x0000 at pen 4, so a COLD call reaches only the early return; title_screen's six-pen ring (0x10b26..0x10b5e) is a closed cycle over pens 3/4/6/8/9/10, shipped 0x0300/0x0000/0x0200/0x0400/0x0300/0x0200, and cycle_palette only RELOCATES a level rather than changing it — so the only pen-4 values the game's own data can ever circulate are 0 and a level in {2,3,4} in some combination of R/G/B. Of the hand-written cases only 0x003 (blue level 3) is in that set; 0x0f0/0xf00/0x0a0/0x123/0xffff/0xf001/0x8000/0x1000 and levels 1 and 5..15 are constructed. They are legitimate — they are well-formed ST colour words, and the routine's arithmetic is data-independent — but they are NOT evidence about reachable game states, and the level sweep's value is pinning the masks and shifts, not proving the game gets there. title_screen IS ported now (above) and its battery runs the ring, including one case that reads the cycle's own output back out of pen 6 — which is what pins the two routines' ORDER. THE PENS IT STAGES ARE STILL CONSTRUCTED, though, and in the same way: its 16-pen table is synthetic (the fifteen non-cycled pens carry a set bit-12 nibble no real ST colour word has), and the one hue it circulates through the composed pass is 0x0300, which IS in the shipped set. So the ring is exercised; the shipped 6-tuple as a whole is still not |
| `0x11c24` | `poll_quit_key` | 114 | ✅ verified | no-key / ordinary-key / 251-key sharded fuzz over ASCII x scancode; R/r at stop_pc=0x10006 and Ctrl-C at stop_pc=0x11d4c, EACH PAIRED with a proof the run does not reach rts (a stop_pc run that fell through would stop at the sentinel and pass silently); quit path covers the Dosound ledger, the hiscore_dirty gate, Fopen/Fwrite(0x1a)/Fclose into a staged HIGH.SCO, conterm and both KBDVBASE vectors; a 9-call battery reads Setscreen/Ikbdws x2/Kbdvbase/Super/Setpalette's arguments back out of the oracle's stack, since those traps change no memory; poison. LIMITS: the P/p pause needs a SECOND keystroke, which the console model cannot deliver (one key per run) — its loop is verified separately entered at 0x11d64; the Fopen->Fcreate fallback is UNREACHABLE by construction (os_fcreate = os_fopen + truncate, so both succeed or both refuse) and is reproduced unverified |
| `0x11d9a` | `read_joysticks` | 60 | ✅ verified | VERIFIED IN TWO PIECES either side of the IKBD wait, with a proof neither piece can reach the other; between them the two runs execute every instruction of the ORIGINAL. Each half is a NAMED SEAM its glue drives, so everything inside either is held — AND THE COMPOSITION IS HELD TOO, which took a test of a different kind: no ORACLE run can cross the wait, but the CANDIDATE'S can, because the wait is `volatile` precisely so an interrupt may end it. test_read_joysticks_blocks_until_a_reply_lands runs the whole reconstructed routine on a thread and PLAYS THE INTERRUPT — it polls until the routine's own `clr.l` lands, asserts the routine is STILL BLOCKED a quarter-second later, and only then stores the reply pointer. That is the one test here that is not a differential (there is no oracle run to compare against, which is why it exists), and it pins the sequence clear -> block -> read that the two glues cannot hold between them. THE ROUTING IS CROSSED AND THE DIFFERENTIAL HOLDS IT: ikbd_packet is a POINTER to the two-byte reply, and the routine gives JOYSTICK 1 TO PLAYER 1 (`bsr` at 0x11dc8) and JOYSTICK 0 TO PLAYER 2 (0x11dd2). Pinned by two MIRROR-IMAGE cases pushing the sticks in OPPOSITE directions, so neither reading of the pair can pass both, and by all four shapes of the fire bit — which also rules out the OR title_screen does on the same two bytes. THE ORDER OF THE TWO CALLS IS HELD BY THE DIFF TOO, which is unusual for this reconstruction and is possible only because the calls share memory on exactly one frame: both riders dead, out of lives and past the exit column, so the first to run takes players_alive 2 -> 1 and posts PLAYER 1 GAME OVER into message slot 0 and the second takes it to 0 and posts the shared GAME OVER into slot 1 — swapping the calls swaps both records (measured as a mutant, not asserted as prose). The sticks are dead on that path, so the order case depends on nothing the routing cases prove. THE ROTATED ENTRY is 0x11db0 (the `tst.l`) with a reply staged, diffed at the `rts` — the same rotation title_screen's joystick start and hiscore_joystick_input use, and for the same reason: the prologue clears ikbd_packet, so nothing poked survives a run entered earlier. THE PROLOGUE IS CHECKPOINTED at that same address from 0x11d9a, with its write set EXACTLY the four bytes of the clear and nothing else, over staging in which anything past the wait would be loud (both slots empty, both sticks firing, nobody alive — a run that got through would restart the game), poison MEASURED safe, PAIRED with a never-returns proof at an ARGUED cap: SPIN_CAP is asserted to exceed 4x the instructions a pass that DOES return costs, MEASURED per run rather than assumed. THE EMPTY-SLOT RESTART REACHED THROUGH THIS ROUTINE is diffed BOTH WAYS at control_player's own 0x11e44 checkpoint, each paired with its never-returns proof — from the FIRST call the write set is game_over_flag ALONE, which is what says player 2 is never steered and that read_joysticks stores nothing of its own past the wait; from the SECOND it is player 1's steered record AND game_over_flag, which is what says the first call completed before the second began. Ikbdws changes no memory, so the FOUR words the interrogate pushed (a selector, a length and the command pointer's two) are read back off the ORACLE'S OWN STACK at 0x11dae (selector 0x19, length 0, ikbd_cmd_joyread) — and the reconstruction issues no Ikbdws at all, exactly as init_system's does not, so an on-target build has to re-issue it. THE WAIT IS A `tst.l` and three of its four bytes are pinned by STAGING — WHICH TOOK TWO PROBE BUFFERS, NOT ONE, and one probe was the subtler half of the same mistake the ordinary scratch address makes. A pointer whose HIGH WORD IS ZERO (0x00c000) kills a wait reading `packet[0] | packet[1]`, but its own LOW byte is zero as well, so a wait reading three bytes survived it and the whole suite (MEASURED: 4306 green with the LSB dropped). A second pointer non-zero ONLY in its least significant byte (0x0000f0) kills that one. With both, a wait cut at either end never terminates and the wall-clock deadline turns it into an ORDINARY RED rather than a hung worker — measured, and measured in BOTH batteries, since the wait is one shared function. The two probe addresses have ONE definition (test_input.py), imported by this battery and title_screen's. The fourth byte cannot be reached by any legal pointer in a 0x100000-byte image and is covered against the ORIGINAL'S encoding instead. ITS GLUE REFUSES TWO PACKET POINTERS for two different reasons — empty (the uncapped spin would hang a pytest worker with no output) and outside the image (the oracle's callbacks answer 0 past its size while the candidate would index host memory) — with a positive pair at the bound. The refusal is `ikbd_packet_readable` (src/input.c), ONE predicate title_screen's glue now asks too, so the bound has a single definition and both batteries score a mutation of it. 256-case sharded fuzz over both packet bytes x two independently shaped riders x FULL 32-bit D0/D1, each shard classifying its own cases so a corpus that stopped reaching an arm fails instead of sharding cleanly — AND THE CLASSIFIER READS THE STAGED FLAGS, NOT THE WRITE SET: control_player writes OBJ_TARGET_VX on the live AND the dead path, so a counter watching that address reports 'steered' for a corpus of nothing but corpses (CLAUDE.md's output-sniffing trap). Rewriting it that way IMMEDIATELY CAUGHT A REAL HOLE — a free 16-bit flags draw is 0 once in 65536, so the EMPTY-SLOT arm had never been fuzzed at all; the corpus now draws flags the way control_player's own fuzz does. The one thing only the ORACLE can say — that a corpse really ran off the edge into player_death — is still counted off its writes. THE PROLOGUE'S BYTE-IDENTITY AT ALL THREE SITES — the premise of sharing it at all — is PINNED against the image rather than asserted in prose, so a fourth caller folded in later whose `clr.l` targeted a different word could not be modelled by the shared pair unnoticed. THE IKBD PROLOGUE IS NOW ONE SHARED PAIR (request_ikbd_packet / wait_for_ikbd_packet in src/input.c) rather than a second transcription — the six instructions are byte-identical at 0x11d9a, 0x10ba2 and 0x14538 — so a mutation of either half is scored by BOTH batteries that drive it, this one and title_screen's. Only two of the three, though: hiscore_joystick_input is reconstructed from PAST its wait and so has no prologue to share. 24/24 mutation sweep on the FINAL tree, every mutant forced through a relink; NO survivors, and none killed only by a hang. TWO OF THOSE KILLS WERE SURVIVORS UNTIL AN ADVERSARIAL REVIEW — dropping the interrogate from `read_joysticks`, and deleting the wait outright — and both fell to the interrupt-driver test rather than to a reworded limit; a third (narrowing the wait to three bytes) was never scored at all until the second probe buffer existed. LIMITS: (1) THE WAIT IS UNVERIFIABLE ON THE ORACLE'S SIDE, and that is the limit that remains: the reply arrives on an IKBD INTERRUPT the oracle never runs (TRAP_MODEL.md's limit 1), pinned by test_input.py::test_ikbd_wait_never_ends_from_the_routines_own_entry. So nothing DIFFS the blocking — no oracle run reaches an `rts` to compare a final image at. The CANDIDATE'S blocking is pinned (the driver test above), and the ORIGINAL'S is pinned as its `tst.l`/`beq.s` encoding; what no test can do is show the two agree, because one side cannot execute it. (2) THE TWO LIMITS THIS ROW USED TO CLAIM HERE WERE FALSE, and are recorded because the reasoning that produced them is the trap, not the outcome. It said no case could drive the routine's two halves in sequence, and that deleting the wait was therefore unobservable 'by construction' — both inferred from the ORACLE'S inability to cross the wait and from the glue's need to refuse an empty packet. Neither followed: the candidate runs in-process, and the interrupt the model does not have is a poke from another thread. The lesson is the one this project has now met twice — A LIMIT INFERRED FROM THE HARNESS'S SHAPE IS A HYPOTHESIS, and the cheap experiment comes before the disclosure. (3) `move.l d1,d0` at 0x11dd0 hands player 2's stick over as a FULL LONGWORD, so 24 bits of the caller's D1 ride into control_player, which tests three bits of the low byte and no more — no input can separate the long move from a byte one, so it is pinned against the ORIGINAL'S encoding (the fuzz feeds full 32-bit registers to show the bits really are dead). (4) `addq.w #8,a7` at 0x11e3a drops read_joysticks' return address as well as control_player's, and FOUR would be indistinguishable under the model: the restart ends in a `jmp` into the main loop either way, so neither width ever reaches an `rts`. Encoding again. (5) The fuzz keeps lives at 1..3 so no rider EXHAUSTS one — a player_death that took players_alive to 0 would let the other slot's fire restart the game, which has no `rts` to diff at. That frame is covered by the order case above instead |
| `0x11dd6` | `control_player` | 338 | ✅ verified | steer: 9 stick shapes x 6 flag shapes incl. one with only ONE flap bit set, target vx as a whole-word copy over 0/1/0x7fff/0x8000/0xfffc/0xffff, respawn zeroing, right-beats-left. Dead rider: the exit window over 12 x values x both facings (NOT mirrored — 0x134..0x13b hovers facing left), 0x8000/0xffff pinning the SIGNED cmpi.w; hover ladder over 16 y x 5 vy incl. negatives; the flap TOGGLE from all four states of the pair INCLUDING y exactly on its band's target — a mutation sweep found that hole, since from a set start a toggle and a clear are the same word. RESTART PATH CHECKPOINTED at 0x11e44 (the first of three unported calls): proves both guards route there and that game_over_flag := 1 is the ONLY write on this side; proves nothing past it. PAIRED with a never-returns proof over the SAME registers — which is what caught that pair being staged wrong. Its tail (0x11e50, restart_reset_players) is verified separately at its own entry, likewise paired, over all three two_player_mode arms, and is deliberately NOT called from control_player since check_highscore and init_game sit between (and check_highscore does not come back at all once the game has just set a record). poison only where the poisoned bytes are not also the branch selector (MEASURED: inverting the flags word flips dead<->live) |
| `0x11f28` | `player_death` | 236 | ✅ verified | the cmpa.l/bls bound straddled by one WORD — full-width pinned, unsignedness NOT provable here (bls and ble differ only outside the image, stated in the test); all 12 enemy slots; lives 1/2/3/0x7f/0x80/0xfe x 4 flag shapes; the 0-vs-1 byte wrap; players_alive 1/2/0 — the 0 wraps to 0xff and gets a per-player banner, reproduced not guarded; p1/p2/neither wording+colour by a full cmpa.l; message slot at 0/1/5/23; FULL TABLE -> find_free_message returns 0 and the record lands over addresses 0..0xb (original bug, pinned); 240-case sharded fuzz; poison |
| `0x12014` | `update_objects` | 1522 | ✅ verified | the 12-slot walk (players never visited), bound + stride pinned on the ORACLE's write set — an all-free table stores 0 over 0, which no image diff can see; active_players over 12 shapes; the turn-timer flip incl. 0x80/0xff (cmpi.b+bcs is UNSIGNED). Type 1: the chase boundary DERIVED from (speed_type2<<2)-6 so the shift and subtraction are pinned, and the STALE D2 read over 7 values. Type 2: rng jitter over cursors chosen so `& 3` sees all eight low-bit patterns (pins the mask as 3, not 7); claim over 10 chaser pairs (SIGNED pick, UNSIGNED cap); the sub.w+bge x gap at FULL precision. Type 3: the dive window with player y DERIVED from the wanted reach — the only way the third of three tests executes at all. BIT 12 fully exercised incl. test_removed_beats_a_surviving_egg, which exists because without it deleting the bit-12 TEST just latches the bit one instruction later onto the same tail. Eight shared tails x all 8 combinations of bits 6/10/11. 240-case sharded fuzz. Instruction coverage 366/369 — the 3 misses are the DEAD tail at 0x12546. NO POISON, MEASURED: rng_ptr is written every slot and read back two instructions later, so a poisoned re-run tests a different case and still passes; test_poison_would_test_a_different_function asserts exactly that |
| `0x12606` | `update_eggs` | 590 | ✅ verified | all 14 slots (loop stride + bound), the lava sink at 5 row counts, the hatchable wait over 7 live counts incl. the SIGNED 0x80 and 4 timer residues, all 27 animation states the game can produce, the bounce-up frame, the hatch (6 launch x, 10 altitude bands, 8 rider types, 6 speeds), gravity/terminal speed/x wrap/y clamp incl. the add.w N==V overflow pairs, multi-slot passes, 320-case sharded fuzz. State 4 excluded on purpose — its jump-table record is null and nothing writes that state |
| `0x1285c` | `update_egg_draw` | 184 | ✅ verified | both entries (head + the 0x128c0 tail), each of the four record fields forcing the erase, the never-drawn bchg, 8 x-values across the wrap column, 4 row counts, the empty slot that still commits, the lava rewrite at every row, and the `jmp 0x12612` continuation driving two later slots |
| `0x12914` | `draw_egg_sprite` | 160 | ✅ verified | 4 egg states x 9 shifts x 3 x-values (poisoned), lava line at every row incl. draw_rows=0 -> 256 wrap rows, signed cmpa.l over 6 bounds, 200-case fuzz x 2 shards |
| `0x129b4` | `erase_egg_sprite` | 118 | ✅ verified | 13 shifts x 4 x-values, wrap column either side of 0x130, subq.b 0 = 256 rows, 200-case fuzz x 2 shards, poison on the wrap battery |
| `0x12a2a` | `update_egg_physics` | 640 | ✅ verified | BOTH EXITS. Landing box edges on all 4 sides x every platform slot x every present byte, still-rising/roll-friction/bounce/settle/stuck-spot branches (poisoned), the pixel pass incl. hit_box_a staged before the present test and a staged divu.w overflow, and the platform-edge TAIL JUMP — which discards its own return address, so it is diffed with a second sentinel poked one longword past emu.STACK_TOP and proved by a PAIRED negative (no rts without it) and positive (draw_dst in the write set). 4 edge cases x 11 roll speeds, plus the continuation into a later slot and from the last slot |
| `0x12caa` | `render_objects` | 8 | ✅ verified | the head of the family: a0 = object_table, falls into the body. Whole-table walk with all 14 slots live, each slot's prev_rows asserted written |
| `0x12cb2` | `render_objects_next` | 16 | ✅ verified | the loop tail and the family's ONLY rts. Entered one slot early (that slot proven NOT rendered, the next proven rendered) and at the last slot, where the advance hits effect_table and the write set is empty |
| `0x12cc2` | `render_object_body` | 2232 | ✅ verified | all five branches + both RESTART edges, each proved to re-dispatch by a flag only the re-entered branch can change. Flight: signed-byte troll cap, flap latch, the subq.w word wrap on the rise limit, and BOTH add.w+bge N==V steps. Sprites: 10 flag shapes x 4 poses x both facings, the stride pose (reachable only by the turning snap — 0x132ee wraps the counter at 4). THE DEAD-RIDER EDGE CLIP: all four leaves of btst #12 x sign(vx), asserted on draw_half_select off the ORACLE's image so a case that missed the branch fails, PAIRED with a proof the bit is inert away from the edge. Lava: signed longword bound, the UNSIGNED <= player2 score gate. Edge bump: all 28 shipped records isolated and hit centre. Respawn: lock + SIGNED live-count gates, the grow counter in OBJ_VX's HIGH byte and the spawn offset in OBJ_VY. 320-case sharded fuzz. NO POISON, MEASURED: prev_dst is a screen POINTER dereferenced next frame, so a poisoned one is a wild address and the candidate SEGFAULTS the worker |
| `0x13300` | `check_platform` | 118 | ✅ verified | all 4 box edges inclusive x 36 positions, per-slot platform_present indexing, first-match-wins on overlapping boxes, flap-frame nudge, walk reset on leaving, signed coords; poison on 5 |
| `0x135f4` | `select_sprite_base` | 52 | ✅ verified | both identities +/-1 byte (full-longword cmpa.l), bit-15 facing incl. bit-31 noise, 200-case fuzz; register result (D1) compared, not memory |
| `0x13628` | `flash_spawn_pad` | 116 | ✅ verified | phase = step_timer mod 4 + the phase-1/flags-bit-2 substitution, all 16 plane selects, 11 shifts across LSR.L mod 64, all 4 spawn records, BOTH adda.w sign extensions, 200-case fuzz; poison, 4 screen bases |
| `0x1369c` | `blit_pattern_rows` | 76 | ✅ verified | all 16 plane selects, high bits of d2 ignored, word-sized masks (hi_garbage), the 12-word write set, poison, fuzz |
| `0x136e8` | `draw_object_data` | 212 | ✅ verified | shifts x row counts (0 = 256) x wrap column across x=0x130 signed x half_select; playfield_bottom re-read EVERY row; lava row count from a FRESH draw_rows read (sub.b/neg.b); poison, fuzz |
| `0x137bc` | `draw_object_mask` | 134 | ✅ verified | shifts, row counts, prev_x wrap; erase pass aimed at the record's own fields pins that the wrap column re-reads prev_rows/prev_src but NOT prev_dst/prev_shift; poison, fuzz |
| `0x13842` | `collision_check` | 1712 | ✅ verified | all four sweeps — 383/383 of the routine's instructions executed. Platforms: bit14 + the player-ONLY sound, rows read as the ODD half of the rows word, the divu.w y incl. OVERFLOW, and the corpse that still bumps but never jousts. Joust: pairs meet once (a3 starts one slot on), the enemy bounty by type bits and the whole egg it drops (13 fields, the type climb capped at 3, the 0x88-wave_num BYTE wrap), the player-vs-player 500+2500 with BOTH gates, and the losing player leaving the sweep (proved by a third rider it never reaches). Eggs: slots 2..13 only, the chain over all 4 bonus records incl. the unchecked index at 0x80, a FULL table writing the record over addresses 0..0xb. Pterodactyls: both lance windows to the pixel on their SIGNED and UNSIGNED bounds, and the death block — start_death_anim INLINED MINUS its egg-chain clear, pinned BOTH ways, since a reconstruction that simply CALLED start_death_anim would pass the first assertion alone. 240-case sharded fuzz. poison on ONE case, MEASURED: inverting the flags word raises OBJ_FLAG_RESPAWN and 7 of 8 probed shapes then wrote nothing at all |
| `0x13ef2` | `test_overlap` | 244 | ✅ verified | exg ordering by screen address, add.b y-band byte-wrap, divu.w #$a0 incl. overflow, both column-alignment branches over all 20 cell gaps, signed-byte row clamp, hit ends the sweep, 300-case fuzz x 4 shards |
| `0x13fe6` | `pixel_collision` | 178 | ✅ verified | per-row hit position, spill gated on cursor+shift, 13 wide shifts (LSR.L mod 64) x 2 orderings, subq.b 0 = 256 rows, adda.w sign-extended stride, 400-case fuzz x 4 shards; poison on 3 |
| `0x14098` | `start_death_anim` | 102 | ✅ verified | player-1 identity +/-1 byte (cmpa.l), the 0x280 rise and its SIGNED clamp incl. bit-31 screen bases, bset #13/#12 over 9 flag words, addq.b score wrap; D0 compared via a store stub; poison |
| `0x140fe` | `joust_bounce` | 96 | ✅ verified | 14 gaps across both thresholds (unsigned bcc / signed bge) x 9 velocity pairs, neg.w 0x8000, flags high half not stored, already-correct velocity left untouched; poison on 2 |
| `0x14160` | `score_update` | 6 | ✅ verified | the A0 entry of the alias family; see score_update_p1 |
| `0x14166` | `score_update_p2` | 12 | ✅ verified | the player-2 entry, A0 ignored; same battery |
| `0x14172` | `score_update_p1` | 212 | ✅ verified | the shared body: the promotion sweep's blank skip + SIGNED `< '0'` test over 10 bytes and its stop one short of the units digit (pinned by a string that RUNS the sweep to that bound, not just an all-blank one); the carry sweep at all 7 columns with the UNSIGNED `> '9'` repeat over 0x3a..0xff, the blank->'0' promotion, and the overflow into the string's own colour byte; the extra life for all 11 ten-thousands values, none from any other column, several in one call, the life-count wrap, the sound through play_sound's SIGNED gate (asserted on the Dosound ledger); 200-case sharded fuzz. Every case checked against a Python model of both sweeps, so a vacuous pass fails |
| `0x14246` | `draw_lives` | 8 | ✅ verified | dispatch only: full `cmpa.l` against player2 over 7 A0 values (+/-1 byte, enemies, 0, 0xffffffff). NOTE both bodies reload A0 from a constant, so draw_lives(enemy) draws PLAYER 1's row |
| `0x1424e` | `draw_lives_p1` | 18 | ✅ verified | counts 0..6/0x7f and the SIGNED 0x80/0xfe/0xff, 7 shifts incl. the addi.b byte wrap, 4 caller flag states, 4 screen pointers, A0 ignored, count re-read every position (row painted over its own record), 120-case sharded fuzz; poison |
| `0x14260` | `draw_lives_p2` | 126 | ✅ verified | the shared body through the p2 entry: its own record and glyph string, same battery |
| `0x142de` | `draw_messages` | 126 | ✅ verified | all 24 slots, subq.b timer (0 -> 255), expiry frees the slot AND STILL DRAWS IT in colour 0 (that draw is the erase), players_alive cut-short over 6 counts with the kind-3 exemption, game-over cmpi.l probed on 3 bytes, distinct glyph per slot, 60-case fuzz; poison |
| `0x1435c` | `find_free_message` | 30 | ✅ verified | first-free at slots 0/1/12/23, full table -> 0 (suba.l), every non-zero kind incl. 0x80, 200-case fuzz; A0 compared — it writes nothing, so an image diff proves nothing |
| `0x1437a` | `check_highscore` | 310 | ✅ verified | ALL THREE EXITS OF THE ORIGINAL EXECUTED — instruction coverage 76/76 — two of them diffed at `rts` and the third at a checkpoint; the loop body's C composition is the one thing left unpinned (see the LIMITS at the end). Returning: `tst.b game_over_flag` over 4 values (it returns before reading a score at all, staged with a record-beating one); the leader settled at each of the 7 digit positions in BOTH directions; a score below the record at each of the 7, with the write set proving draw_src is the ONLY thing written. Both `cmp.b` pairs are SIGNED — 0x80 loses to 0x7f — and on the SECOND comparison that decides the EXIT, not the answer: read unsigned it would beat the record and never return. THE COUNTER-DESTRUCTION BUG, PINNED BOTH WAYS AND ON BOTH LOOPS: `move.b #$7,d0` is overwritten by the character on the spot, so this is a WALK — equal strings run PAST the records and the byte after the seventh digit decides (both directions), while a character of 1 stops it and falls through in OPPOSITE directions (player 1 takes the tie at 0x143a8; the record is NOT beaten at 0x143de). MEASURED on the shipped table: two identical player scores walk 79 bytes, far enough that what settles it is PLAYER 2's own digits against ENEMY SLOT 2's — pinned as a test, not as prose. The copy at 0x1440a is the one loop whose counter survives: the write set is exactly 0x183a9..0x183af, the 3 bytes between the name and the digits untouched. NEW RECORD CHECKPOINTED at 0x1448e, the entry loop's head: the Dosound silence on the kit's ledger, hiscore_dirty, the 7-byte copy, fill_screen, the per-player banner, the 16 blanked name columns, text_flags/color/bg, repeat_delay, cursor/touched and the letter as ONE word store (' ' plus the NUL that terminates it), and draw_hiscore_cursor — over a noise framebuffer, PAIRED with a never-returns proof at a 200,000-instruction cap chosen against the 35,429 the checkpoint itself costs, so it says "inside the loop", not "slower than the cap". THE LOOP CANNOT COMPLETE ONE PASS from that entry — hiscore_joystick_input blocks in its IKBD wait on the first pass whatever is staged (pinned positively: the run reaches 0x1454e and really does clear ikbd_packet) — so it is verified entered at the colour-cycle tail 0x14494 and stopped at 0x14490: the pass count, one flash step, the `subq.b`/`bne`, the branch back at 0x144ae and the keyboard poll, with 'A' typed to prove that poll is the real reader. 144-case sharded fuzz over both comparisons, every case classified by an INDEPENDENT Python model of the walk (verdict AND distance), with the corpus asserted to reach all three verdicts and at least one over-long walk so a corpus that stopped covering the bug fails instead of sharding cleanly. poison on both returning paths and on the entry screen (MEASURED: poisoning inverts the record's own digits, which makes it smaller and so still beaten). 28/33 mutation sweep, every mutant forced through a relink; the FIVE survivors fall into four classes, all of them named below rather than left implied. LIMITS: the 16,000-spin `move.w #$3e80,d0` delay has NO memory effect, so the differential cannot see the count, the loop, OR THE CALL — deleting `hiscore_flash_delay()` outright leaves the suite green (the first survivor class, measured). Only the ORIGINAL'S instruction encoding is pinned, as it is for the four `bsr` targets, the two banner pointers and the `addq.w #4,a7` stack drop; the reconstruction's own delay is honestly unpinned, and on-target that is the entry screen's whole pacing. The pass count is reloaded with 1 every time round the loop, so the `bne` at 0x144ac is never TAKEN and a straight-line reconstruction is the second survivor class (measured). `addq.w #4,a7` at 0x143e0 discards the caller's return address — stack, which the diff drops; the C reports CHECK_HIGHSCORE_ENTERED instead. And the loop's own exit is unreachable under the model on BOTH sides: the setup clears hiscore_touched, so the RETURN/fire that would end the entry is ignored on the pass that follows and the console has no second key — which is why g_check_highscore refuses the loop rather than forwarding into it. THE PRICE OF THAT REFUSAL, STATED RATHER THAN HIDDEN — and it is wider than the loop: NO ORDERING ANYWHERE IN THE ENTRY LOOP IS HELD BY THE DIFFERENTIAL ON THE C SIDE. Nothing executes `check_highscore`'s own `for (;;)`, so neither its composition NOR its order is pinned there: deleting either reader call from it leaves the whole suite green (the third survivor class, measured both ways), and CHECK_HIGHSCORE_RESTART has no producer any case can reach. The rotated pass `g_hiscore_entry_pass` does hold PRESENCE — dropping its flash step diverges on A_hiscore_flash and dropping its keyboard poll on the letter it types, both measured — but NOT ORDER: the two steps touch disjoint memory (draw_x/draw_y vs the letter and the screen, neither reading the other), so SWAPPING THEM IS ALSO GREEN (the fourth survivor class). Both orders are transcribed from the disassembly and asserted against the ORIGINAL's four `bsr` encodings instead; no oracle run can execute a whole pass to hold the C to them. Finally `walk_scores` is unbounded, faithfully: reaching the end of the image is UB on the host, which during the sweep showed up once as a crashed pytest worker rather than a failing case |
| `0x144b0` | `flash_hiscore_color` | 36 | ✅ verified | counters 0/1/6/7/8/0x7fff/0x8000/0xffff, addq.w wrap pinned by a sentinel in the next word; the Setcolor pen and colour word read back out of the oracle's own trap arguments (the palette write is off-image); poison |
| `0x144d4` | `hiscore_key_input` | 100 | ✅ verified | backspace at columns 1/2/8/15 plus the 0x8000 subq OVERFLOW (bge is N==V, so it clamps) and the column-0 clamp; RETURN ignored untouched and at stop_pc=0x10006 for 3 non-zero draw_rows values, with a never-returns companion; all 26 upper-case, all 26 lower-case folded, space; 19 rejected bytes separating the UNSIGNED fold threshold from the SIGNED range tests; the last-column clamp and the 0xffff-wraps-to-0 UNSIGNED clamp; 256-key sharded fuzz; poison |
| `0x14538` | `hiscore_joystick_input` | 288 | ✅ verified | VERIFIED FROM 0x1454e — entered at the IKBD wait loop with the reply staged. The prologue (clr.l ikbd_packet, the Ikbdws interrogate, the blocking wait) is NOT verified and cannot be: the routine clears the packet before waiting, so no poked constant survives it. From the packet read on: stick owner over 6 values incl. player2|0xffff0000 (the full cmpi.l), fire gated on draw_rows and set AFTER the test, centred stick clearing the counter, the subq.b repeat counter incl. 0x80's overflow into blt, both letter wraps in both directions with the second test RE-READING the byte, left/right into the shared cursor tails, direction priority, 256-stick sharded fuzz; poison |
| `0x14658` | `draw_hiscore_cursor` | 78 | ✅ verified | all 16 columns x both parities, the 8-cell rule with noise past its end, sign-extended column offset, 4 screen bases, 200-case fuzz; poison |
| `0x146a6` | `draw_hiscore_entry` | 80 | ✅ verified | 16 columns, letters incl. 0x00 and 0xff, the string really starting at draw_dst_off, BOTH sign-extensions (raw-cursor name index, and the bit-0-cleared screen offset), 200-case fuzz; poison |
| `0x146f6` | `lava_troll` | 706 | ✅ verified | signed wave gate, subq.b+bge step timer (0 and 0x80 both reload); scan rejection by flags/signed reach row/the two-ended pit band, first-match-wins, all 14 slots; the raise BUILDS the state word; the tracking window measured BOTH ways round the 320-px screen; the UNSIGNED contact window with the grab sound as witness; climb, retract with subq.w #8+bge at 0x8000/0xfff8/0xffff; the hold block's signed escape line paying only a PLAYER (proved by the carry score_update leaves), and the respawn/in-lava drop whose clr.w troll_state is DEAD; 4 screen bases; 200-case fuzz x 4 shards |
| `0x149b8` | `troll_erase_hand` | 122 | ✅ verified | early-out needs ALL THREE of src/dst/shift, ror.l mod 32, signed-word rows (0/0x8000 draw nothing), playfield_bottom re-read EVERY row (pinned by a blit that overwrites the surface mid-run), self-overlap read-before-write, 200-case fuzz; poison |
| `0x14a32` | `troll_draw_hand` | 136 | ✅ verified | bit-0 gate on the whole longword, LSR.L mod 64, lava clip re-read per row, all four planes read before any write over 6 overlap deltas, 200-case fuzz; poison |
| `0x14ada` | `update_pterodactyl` | 1470 | ✅ verified | ALL FIVE STAGES. Scheduler: the tick is ABOVE the slot walk (one per frame, not four), the `tst.w`/`bgt` gate over 0x7fff/0x8000/0xffff — 0x8000 decrements to 0x7fff and holds the schedule off LONGEST — the one-bit LOGICAL `lsr.w`, and the reload that wraps NEGATIVE and so arms a second slot in the same frame (two, then its own reload stops the cascade). Build: both entry edges off bit 0 of the byte the ADVANCED cursor points at, including across rng_advance's wrap, with the nudge proved to be the flags word it has just made; the entry dst proved a DEAD store over two screen bases (rows is cleared first, so the first erase draws nothing); the cry through play_sound's SIGNED gate on the Dosound ledger; the `addq.b` spawn-count wrap. Platform sweep: both x bounds and the vertical one to +/-1 SIGNED, first-match-wins, an absent platform still stepping its bump record, the box RESTAGED per pass, the `move.b #$1` normalising platform_present, and a corpse bounced BEFORE it may die. Each arm pinned to the field it does NOT write, which is what makes them the vehicle for the move battery. Separation: both bands inclusive, later slots only (three stacked birds give 0x44/0x40/0x3c, which a backwards-looking scan cannot), and the `sub.w`+`blt` proved a FULL-PRECISION compare while the band that follows reads the TRUNCATED word — 0x8000 against 0x7000 nudges where the truncated sign would leave both alone. Leaving: all three routes in (the third a PAIR), the one-way bit, both exit columns through `divu.w #$6`, the release on an EQUALITY, and the rightward/leftward ASYMMETRY. Hunting: the fade gate on both counters, each player's own `btst`, the dwell `subq.b`+`bgt` at 0x80 (decides that frame, not 127 later), the nearer-row pick with the squared gaps compared as SIGNED WORDS (a 0xb6 gap squares to 0x8164 and beats a gap of 2), the dive's pose FREEZE at pose 2 — the only path that branches past the shared advance — and the climb step flattened to 1. Death: the pose flip on the stored byte, the flap alternation as a SIGNED word compare, the release that clears the flags and so lets the pose step once more, the bounty's address and shift over word-wrapping x/y, and a FULL table losing the popup rather than writing over addresses 0..0xb the way find_free_message's callers do. Move/draw: the x wrap on a SIGNED 0x140 bound (0x7ffa+6 stays at 0x8000), the leftward `bge` = N==V at 0x8000 for every step, the down clamp UNSIGNED and the up clamp N==V, the clip suppressors at every cell-wrap boundary, the `subq.b`+`bge` fade clamping 0x80 to 0, the frame table indexed by the pose bits with the facing stride, and the erase proved to use the PREVIOUS frame's record AND to run before the draw — the latter from a case whose two blit rectangles COINCIDE over a zero mask and a solid sprite, since staging them apart pins only which record is read. 240-case sharded fuzz over six shapes, each shard classifying its own cases off the ORACLE's writes so a dead sixth of the corpus fails instead of passing quietly. Instruction coverage 353/353 — every instruction of the routine executed. 30/30 mutation sweep, no holes. POISON on two batteries only, MEASURED: the routine branches on nearly every byte it writes, so an inverted image runs a DIFFERENT case (test_poison_would_test_a_different_function pins that) — the exceptions are the bounty's message record and the framebuffer, neither of which it reads back |
| `0x15098` | `blit_mask_wide` | 116 | ✅ verified | shift counts around 0/16/32/0x3f, non-positive counts incl. 0x8000 (BGE = N==V), sign-extended dst_off, middle cell masked twice per row; record-overlap case pins read-once; poison, fuzz |
| `0x1510c` | `blit_sprite_planes` | 202 | ✅ verified | shifts x all 8 clip combinations x row counts; read/write phase order over 7 source deltas x 5 shifts — the trailing cell's 4 words are RE-READ after the first two passes write; suppressors re-tested per row; poison, fuzz |
| `0x15226` | `ptero_avoid_platform` | 80 | ✅ verified | x band +/-1 over 9 values, row band swept 28 rows, D1 high half survives on all 3 exits (records carry a non-zero divu remainder), absent platforms, divu overflow; poison on 3 |
| `0x15276` | `ptero_spot_player` | 72 | ✅ verified | row band both signs, 26 x-gaps straddling both thresholds from both directions, sound priority incl. the drop path, word-wrap coords, other flag bits ignored; poison on 2 |
| `0x17438` | `dissolve_platforms` | 322 | ✅ verified | 1-based kind incl. its sign-extended word truncation (base is 0x119c4, one record BELOW platform_sprites), setup sweep vs running slot, subq.b extents on both, all-zero noise -> all-ones, rng cursor across the signed wrap, 4 slots threading one cursor, all THREE re-read semantics; 120-case fuzz, 4 screen bases |
| `0x1757a` | `raise_floor` | 64 | ✅ verified | rows-left is a zero test not a sign test, subq.b timer (0 = 0xff), and the two strips at cells 0-4 / 15-19 — the second call's address is where the FIRST left a1 (+0x28) plus 0x50, not 'the two halves' |
| `0x175ba` | `paint_floor_row` | 36 | ✅ verified | uniform + noise cells, poison, write set pinned to exactly the 5 plane-3 words |
| `0x175de` | `animate_ground_shrink` | 372 | ✅ verified | signed latch gate, 3-frame timer, flame-frame wrap, sink gate as a FULL-WORD compare, sink-outranks-climb, the climb's deliberate non-refresh of the latch, ground-edge wrap, SIGNED rollover at 0x7fff/0x8000 (subq.w+bge is N==V; the left flame is NOT symmetric), 150-case fuzz, 4 screen bases |
| `0x17752` | `blit_sprite_mask` | 80 | ✅ verified | shared battery with blit_sprite: shifts, signed low-byte cell select with the high byte swept, row counts, hi_garbage on d7, full-longword dst_off, self-overdraw read-before-write, poison, sharded fuzz |
| `0x177a2` | `blit_sprite` | 154 | ✅ verified | as blit_sprite_mask plus the colour data ORed behind the mask (shifted, not rotated); same battery |
| `0x1783c` | `wave_manager` | 2660 | ✅ verified | BOTH CHUNKS — dispatcher 0x1783c..0x17e79 (1598 B) + wave start 0x17012..0x17437 (1062 B), reached only by one beq.w and ending in its own rts; the linear sweep hides the split. Hold: message generation over all 24 slots, all 4 ptero slots, the wave-3 latch as a SIGNED word. Announce: 13 banners, the two-digit split as a SIGNED compare, a FULL table sending 12 bytes to 0..0xb. Bonus: the survival 3000 routed by player 1's flags WORD and recoloured AFTER score_update returns — pinned by drawing the HUD row ON the banner's own record, since the write set records final values not order. Wave start: the 0x33->0x29 wrap, effect_table from old AND NOT new with 1-BASED kinds and its 4-slot overrun, the layout longword at a SIGN-EXTENDED byte index (past 0x7f reads BELOW the table), the EGG SCATTER (the divu REMAINDER, the +/-8 spread window, types claimed by subq.b+bge at 0x80 leaving all three counts at -1 — which is what stands the rider loops down), and type 2's INVERTED facing test. 240-case sharded fuzz. 16/16 mutation sweep, 2 of them real holes closed. LIMIT: the pterodactyl CLEAR loop is unobservable — a non-zero flags word holds the countdown, so no run reaching it can have one and it writes 0 over 0 |
| `0x182a2` | `pos_to_screen` | 52 | ✅ verified | 5 screen bases x 14 x-values x 14 y-values (incl. the y>=205 adda.w sign flip) + 800-case fuzz, poison on 5 |
| `0x182d6` | `screen_to_pos` | 42 | ✅ verified | 5 screen bases x 18 offsets (incl. divu.w overflow) x 9 shifts + 800-case fuzz, poison on 4; the x22 scale bug pinned explicitly |

## Notes on what the verified eight turned up

- **`make_fill_pattern`** is degenerate in the shipped binary: four byte-identical `btst #0,d0 /
  or.l #$ffff0000,d1` blocks, and a `d2` that is cleared and never written. Only bit 0 of the
  colour has any effect. Reproduced exactly — see the comment in `src/fill.c` and the `cmt` in
  `../names.txt`.
- **`screen_to_pos`** carries an original bug: it scales the recovered cell index by `mulu.w #$16`
  (22) where inverting `pos_to_screen` needs 16, so it does not round-trip for any x >= 16. The
  routine is unreferenced in the whole image. Reproduced, not fixed.
- **`rng_advance`**'s two constants are *relocated* longwords: the listing's raw `#$7832` / `#$0`
  are really `0x17832` and `0x10000` at the 0x10000 load base. The compare is signed.
- **`pos_to_screen`** folds both offsets in with `adda.w` — the low word only, sign-extended — so
  the address runs backwards from y = 205. Off-screen, but reproduced.
- **The three blits** count their columns and rows with `subq.b`: only the low byte of each word
  argument is the count, and 0 means 256 passes. They also re-read `cols` from the caller's frame
  on **every** row (the row branch targets the `move.w 12(a7),d0`), while `rows` is read once — so
  a rectangle that overwrites its own argument block changes width mid-blit. `blit_from_args` in
  `src/blit.c` models that; `test_cols_is_reread_every_row` fails if it is hoisted.
- **`fill_pattern_n`** counts with `subq.w`: a count of 0 fills 65536 cells (512 KiB).

## Unpinned on target, and why

`docs/on-target-execution.md` ("The observable surfaces") makes it a rule that every on-target
change names the surface which would catch its failure, and that a change naming none is recorded
here rather than left implied. These are the ones the `atari/` shim carries. None is a defect; each
is a limit of what a headless run can witness, stated so a green is not read as more than it is.

- **The IKBD teardown reset is confirmed neither SENT in full nor ACTED ON.** `shim_teardown` sends
  the game's own `$80 $01` / `$14`, and the strongest available read-back is `RB_IKBD_TX_DRAINED` —
  the ACIA's transmit data register emptied. That is *two* steps short of the claim one would like.
  First, on a 6850 TDRE goes high when the data register is copied into the **shift** register, so
  the last byte is still being clocked out for another ~1.28 ms at 7812.5 baud: what is proven is
  that every byte but the final one has left the machine and the final one is on its way. `Pterm`
  can still be reached with that byte in flight (the ACIA completes it regardless, but nothing here
  witnesses that). Second, a byte that fully leaves still says nothing about the controller obeying
  it — the keyboard has no readable "I am out of interrogation mode" state, and a reset it ignores
  reads exactly like success. The evidence that closes both is outside the process: the desktop
  having a mouse after the game exits, which is the GUI check in `atari/README.md` §13.
- **`conterm`'s high bits are unpinned because no ROM sets them.** `RB_CONTERM_CLEARED` reads the
  whole byte back against `saved_conterm & CONTERM_KEEP`, which is the exact assertion — but
  measured at `joust_main`'s entry, `$484` is `0x07` on **both** EmuTOS and TOS 1.04, so that
  expression is 0 and a mutation that clobbers the byte (`SYS_CONTERM = 0`) is indistinguishable
  from the correct `&=`. The check is right; the machine provides no data that reaches the
  difference. It would bite on a machine whose `conterm` carries a bit above the low three.
- **The play build's title PALETTE is asserted for shape, not value.** `smoke.py play` proves the
  shifter is in ST low and holds sixteen pens that are neither still the desktop's nor a degenerate
  table. It does not prove they are the *right* pens: the values are pinned for the `framediff`
  build by two surfaces at six anchors, and carrying that reference into this mode would need a
  second binary running beside it.
- **The play build's EXIT is not asserted at all.** That build has no scripted key and no frame
  limit, so a headless run is killed by `--run-vbls` while it is still resident and still hooked
  into TOS. Its log is scanned for faults and halts (`check_faults`), but the exit status is not —
  the hand-back is covered by `quit`, `quittitle` and `restart`, which run the same `shim_teardown`.
- **The rendered picture is compared at frame 1 only.** Hatari does not render every frame under
  `--fast-forward`, and the shipped side's deep-anchor screenshots are not reproducible run to run
  (`atari/README.md` §11 carries the measured table). Every other anchor is pinned by memory, the
  pens and the hardware-state vector; only the *displayed* comparison stops at frame 1.
- **The hand-back ORDER is unpinned — the sweep asserts final state, not sequence.** Every one of
  the fifteen read-backs is a comparison made *after* the write, so a `shim_teardown` that restores
  the same five values in a different order passes all of them. Measured, not supposed: dropping the
  `SYS_NVBLS = 0` detach and restoring the queue **length before the pointer** is green on every
  surface this project has — read-backs, memory, pens, hardware vector, rendered picture, timeline
  and exit status. The order is load-bearing anyway (`atari/README.md` §7: the queue must go back
  before the KBDVBASE vectors, or `joy_handler` keeps chaining `$16` while TOS walks a half-swapped
  queue), and the window it opens is a handful of instructions wide, so a vblank has to land inside
  it. Nothing here can witness that. Pinning it would need an ordered ledger of the writes —
  Hatari's `--trace` over the system-variable addresses — which is the timeline surface applied to
  memory rather than to hardware, and does not exist yet.
- **The read-back sweep is Joust's, not the kit's.** `projects/buggyboy/recreate/render/atari/`
  writes the same surfaces (KBDVBASE vectors, `conterm`, `_vblqueue`) and has no read-backs. The
  mechanism — bit registry, two words, the exact-mask assertion, the C-source bridge — is
  game-agnostic and belongs in `tools/recreate_kit/`; it is not there yet.
- **`atari/tos.ld` carries the same latent `.bss SUBALIGN(1)` fault Wonder Boy hit, and
  `atari/mkprg.py` has no fixup filter.** Byte-packing `.bss` does not only abut it to text+data; it
  strips the word alignment the 68000 requires, so a longword datum lands on an odd address whenever
  the object before it has odd size — an **Address Error** at the first store, not a relocation bug,
  and nothing in the C can defend against it (`SUBALIGN` overrides a variable's own
  `__attribute__((aligned))`). Joust is even by luck today. The day this link grows an odd-sized
  `.bss` object — or adopts `-ffunction-sections`/`--gc-sections`, which reshuffles `.bss` on the
  linker's reachability walk and was worth ~15 KB of `.text` next door — it dies at the first
  store to whichever datum landed odd.
  Wonder Boy's fix is `SUBALIGN(2)`, one padding byte per object; its `mkprg.py` also classifies
  every `--emit-relocs` fixup against the loaded sections, reads each surviving one back out of the
  flat binary to check it holds its own target, and refuses through `SystemExit` rather than
  `assert` (which `python3 -O` strips — reproduced: the old file wrapped a corrupted `.PRG` and
  exited 0). Both files are copies of Joust's and have now DIVERGED from them. See
  `projects/wonderboy/recreate/STATUS.md`, "## Performance", 2026-08-26.
