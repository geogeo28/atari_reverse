# Joust (Atari ST, Williams/Atari; Gamex release). Depacked from JOUSTS.CTE via
# tools/depack_gamex.py. Ghidra addresses = image offset + 0x10000. No DRI symbols.
# 75/75 functions named, and as of this commit NONE is still inferred: every name has been
# confirmed by reading the body (decomp.c + 68000 disassembly). The "# ctx" tag (inferred from
# behaviour/context, refinable) is no longer used by any line; the last two were discharged by
# reconstructing 0x175de and 0x13628 and rendering their sprite data — one name held, one did not.
#
# NOTE: many names here were corrected after reading the asm — the XBIOS opcode
# table in tools/prg_dis.py and tools/ghidra_scripts/AtariOsTrapAnnotate.java WAS
# wrong (0x20 is Dosound, not Supexec; 0x1c Giaccess and 0x19 Ikbdws were missing;
# 0x1f/0x21/0x28/0x2a were decimal-as-hex mistakes). That mislabelled the whole
# sound driver as palette/keyboard/graphics code. See the cmt lines below.
# Both tables are CORRECT as of this commit — trust the annotations again; the
# lesson is recorded in docs/tos-os-calls.md's trap-table warning.

# ==== entry / init ====
fn 0x10000 _start
fn 0x10080 init_system
fn 0x105f0 init_game
fn 0x10aae title_screen
fn 0x104b2 init_video

# ==== main loop: game logic ====
fn 0x12014 update_objects
fn 0x12606 update_eggs
fn 0x1285c update_egg_draw
fn 0x12caa render_objects
fn 0x12cb2 render_objects_next
fn 0x12cc2 render_object_body
fn 0x13842 collision_check
fn 0x14ada update_pterodactyl
fn 0x1783c wave_manager
fn 0x146f6 lava_troll
fn 0x142de draw_messages
fn 0x1437a check_highscore
fn 0x1033e count_objects_and_pad
fn 0x11dd6 control_player
fn 0x11f28 player_death

# ==== player / object mechanics ====
fn 0x140fe joust_bounce
fn 0x13fe6 pixel_collision
fn 0x13ef2 test_overlap
fn 0x14098 start_death_anim
fn 0x13300 check_platform
fn 0x15226 ptero_avoid_platform
fn 0x15276 ptero_spot_player
fn 0x129b4 erase_egg_sprite
fn 0x12a2a update_egg_physics
fn 0x12914 draw_egg_sprite
fn 0x149b8 troll_erase_hand
fn 0x14a32 troll_draw_hand

# ==== scoring / lives / messages ====
fn 0x14172 score_update_p1
fn 0x14166 score_update_p2
fn 0x14160 score_update
fn 0x14246 draw_lives
fn 0x1424e draw_lives_p1
fn 0x14260 draw_lives_p2
fn 0x1435c find_free_message

# ==== high-score entry ====
fn 0x146a6 draw_hiscore_entry
fn 0x14658 draw_hiscore_cursor
fn 0x144d4 hiscore_key_input
fn 0x14538 hiscore_joystick_input
fn 0x144b0 flash_hiscore_color

# ==== graphics primitives ====
fn 0x1042a blit_copy
fn 0x10456 blit_or
fn 0x10482 blit_andnot
fn 0x17752 blit_sprite_mask
fn 0x177a2 blit_sprite
fn 0x136e8 draw_object_data
fn 0x137bc draw_object_mask
fn 0x1369c blit_pattern_rows
fn 0x13628 flash_spawn_pad
fn 0x15098 blit_mask_wide
fn 0x1510c blit_sprite_planes
fn 0x175ba paint_floor_row
fn 0x17438 dissolve_platforms
fn 0x102e2 fill_screen
fn 0x102fe make_fill_pattern
fn 0x10334 fill_pattern_n
fn 0x10700 draw_string
fn 0x1052e draw_platforms
fn 0x1757a raise_floor
fn 0x175de animate_ground_shrink
fn 0x182a2 pos_to_screen
fn 0x182d6 screen_to_pos
fn 0x135f4 select_sprite_base

# ==== palette / colour ====
fn 0x10c46 xbios_setpalette
fn 0x10c56 cycle_palette

# ==== sound (XBIOS Dosound / Giaccess — NOT palette/keyboard, see header) ====
fn 0x10a56 play_sound
fn 0x10a8a snd_poll_done
fn 0x1096e snd_tone_sweep

# ==== input / rng / quit ====
fn 0x11d9a read_joysticks
fn 0x11c24 poll_quit_key
fn 0x105c2 rng_advance

# ==== variables: screen / geometry ====
var 0x10dde screen_base
var 0x10d60 playfield_bottom
var 0x10d66 ground_anim_timer
var 0x10d64 floor_step_timer
var 0x10d65 floor_rows_left
cmt 0x10d66 ground_anim_timer: a BYTE countdown, not a screen pitch. init_video sets it to 0x28 (0x106ce); animate_ground_shrink decrements it (subq.b, 0x175ec) and resets it to 3 (0x175f6) each time it fires — so the ground burn steps every 3rd call after a 40-frame delay. It was named bytes_per_row and UNTAGGED (i.e. recorded as confirmed); that was wrong — an ST row pitch is 0xa0, a word, and is never decremented. Caught when the world-layer reconstruction needed it and include/world.h named it independently.
var 0x10d68 ground_anim
var 0x10d84 ground_anim_next
var 0x10cd2 palette
var 0x00484 conterm
var 0x10d22 conterm_save
var 0x10d18 saved_mousevec
var 0x10d1c saved_joyvec
var 0x10d20 saved_rez
var 0x10d24 ikbd_cmd_reset
var 0x10d26 saved_palette

# ==== variables: game state ====
var 0x10cf2 players_alive
var 0x10cf3 wave_num
var 0x10cf4 wave_num_text
var 0x10d08 game_phase
var 0x10d09 active_players
var 0x10d11 two_player_mode
var 0x10d12 game_over_flag
var 0x10d0a live_object_count
var 0x10d0b egg_count
var 0x10d0c message_char_count
var 0x10d0d ptero_spawn_count
var 0x10d02 egg_wave_countdown
var 0x10d03 team_wave_countdown
var 0x10d04 ptero_wave_countdown
var 0x10d05 gladiator_wave_countdown
var 0x10d06 player_conflict_flag
var 0x10d07 first_dismount_owner
var 0x10dc1 collision_hit

# ==== variables: tables ====
var 0x10f36 object_table
var 0x10f84 player2
var 0x10fd2 enemy_objects
var 0x10f38 p1_x
var 0x10f3a p1_y
var 0x10f86 p2_x
var 0x10f88 p2_y
var 0x1137a effect_table
var 0x113ba pterodactyl_table
var 0x117b4 platform_table
var 0x10cfa platform_present
var 0x117b8 ground_x0
var 0x117ba ground_x1
var 0x117f4 platform_edge_table
var 0x11964 spawn_points
var 0x10d14 spawn_point_cursor
var 0x10d13 respawn_lock
var 0x10d4e snd_owner
var 0x10d5e hunter_counts
var 0x10cf6 wave_num_tens
var 0x10cf7 wave_num_units
var 0x119b4 egg_bonus_table
var 0x119d4 platform_sprites
var 0x1899a egg_sprite_still
var 0x18a0a egg_sprite_roll_left
var 0x18a7a egg_sprite_roll_right
var 0x18b9a rider_sprite_type1
var 0x18aea rider_sprite_type2
var 0x18c4a rider_sprite_type3
var 0x11944 spawn_pad_colors
var 0x1194c spawn_pad_pattern
var 0x18636 flame_sprites
var 0x1922a death_sprite_p1
var 0x193da death_sprite_other
var 0x10e16 message_table
var 0x11774 sound_table
var 0x11b58 wave_layout_table
var 0x17e7a egg_spread_scratch
var 0x1150f snd_list_silence
var 0x11d56 ikbd_cmd_mouse_rel
var 0x102c8 fname_highsco
var 0x18396 hiscore_name
var 0x1861b str_life_blank
var 0x1861d str_life_p1
var 0x18629 str_life_p2
var 0x185c5 str_game_over
var 0x185d8 str_player1_over
var 0x185ed str_player2_over
var 0x18388 hiscore_dirty

# ==== variables: per-wave difficulty ====
var 0x10d54 wave_layout_mask
var 0x10d55 wave_type1_count
var 0x10d56 wave_type2_count
var 0x10d57 wave_type3_count
var 0x10d58 speed_type1
var 0x10d5a speed_type2
var 0x10d5c speed_type3
var 0x10ddc flap_delay
var 0x10dfa spawn_interval
var 0x10dfc spawn_timer

# ==== variables: fonts ====
var 0x17eba font_large_glyphs
var 0x18192 font_small_glyphs

# ==== variables: sprite draw scratch (set by the caller, read by the blitters) ====
var 0x10de8 draw_dst
var 0x10dec draw_x
var 0x10dee draw_y
var 0x10df0 draw_src
var 0x10df4 draw_shift
var 0x10df6 draw_rows
var 0x10df8 draw_dst_off
var 0x10dc2 draw_half_select
var 0x10d0e draw_clip_cell0
var 0x10d0f draw_clip_cell1
var 0x10d10 draw_clip_cell2
var 0x11a54 egg_sprite_ptrs
var 0x10da0 hit_box_a
var 0x10db0 hit_box_b
var 0x10dc0 hit_rows

# ==== variables: text engine ====
var 0x10e0a text_ptr
var 0x10e0e text_shift
var 0x10e0f text_color
var 0x10e10 text_bg_color
var 0x10e11 text_flags
var 0x10e12 text_x
var 0x10e14 text_y

# ==== variables: sound / input / rng ====
var 0x10d4c snd_priority
var 0x10d48 snd_sweep_pitch
var 0x10d4a snd_sweep_volume
var 0x10d52 palette_cycle_ctr
var 0x1415e repeat_delay
var 0x10e06 ikbd_packet
var 0x1145b ikbd_cmd_joyread
var 0x10dfe rng_ptr

# ==== variables: lava troll ====
var 0x10dc4 troll_state
var 0x10dc6 troll_prev_dst
var 0x10dca troll_prev_src
var 0x10dce troll_prev_shift
var 0x10dd0 troll_x
var 0x10dd2 troll_y
var 0x10dd4 troll_target
var 0x10dd8 troll_prev_rows
var 0x10dda troll_frame
var 0x10ddd troll_step_timer
var 0x14aba troll_sprite_table

# ==== comments: top level ====
cmt 0x10000 Entry: init_system, init_game, title_screen, init_video; then an infinite per-frame loop. hiscore_key_input's RETURN handler jumps back to 0x10006 (init_game) to restart the game.
cmt 0x10080 init_system: bail out if Getrez()==2 (mono), install the IKBD mouse/joystick vectors via Kbdvbase (saving the old ones in saved_mousevec/saved_joyvec), save+mask conterm (0x484), record Physbase in screen_base.
cmt 0x105f0 init_game: copy the state templates (0x114f8 -> players_alive.., 0x1145c -> object_table), relocate the two score-text pointers by screen_base, clear the message/pterodactyl/effect tables, set geometry (ground_x0/x1 = 0..0x13f) and playfield_bottom = screen_base+32000.
cmt 0x10aae title_screen: draw the title bitmap (0x23aae..0x2b7ae) to screen_base, three draw_string lines, cycle_palette + rotate 6 palette entries, then Bconin: '1'/'2' select 1P/2P (players_alive + two_player_mode); a 400-tick timeout falls through to the attract loop.
cmt 0x104b2 init_video: fill_screen, draw the HUD separator rows, then draw_platforms + score/lives redraw + snd_tone_sweep (the start-of-game sound).

# ==== comments: object model ====
cmt 0x10f36 object_table: 14 slots x 0x4e bytes, ending at effect_table. Slot 0 = player 1, slot 1 = player 2 (0x10f84), slots 2..13 = enemies (enemy_objects, 0x10fd2). Fields: +0 flags.w, +2 x.w, +4 y.w, +6 vx.w, +8 vy.w, +0xa anim timer.b, +0xb step timer.b, +0xc target vx.w, +0xe flap frame.w, +0x10/+0x12 prev x/y, +0x14 prev dst.l, +0x18 prev src.l, +0x1c rows.b, +0x1d shift.b, +0x1e EGG state.b, +0x20/+0x22 egg x/y, +0x24/+0x26 egg dx/dy, +0x28/+0x29 egg timers, +0x2a egg dst.l, +0x2e egg src.l, +0x32 egg rows.b, +0x33 egg shift.b, +0x35 egg-chain counter, +0x36 score text ptr.l, +0x3a score text shift.w, +0x3c..+0x44 ASCII score digits, +0x49/+0x4b AI timers, +0x4c lives.
cmt 0x12014 update_objects flags word (object+0): bits0-1 = enemy type 1/2/3 (each has its own speed: speed_type1/2/3), bit2 = is a player, bit4 = grabbed by the lava troll, bit6/bit11 = flapping, bit7 = awaiting respawn, bit8 = falling into the lava, bit9 = standing on a platform, bit12 = removed (being taken off the board; set at 0x124ac, tested at 0x12438 and 0x12faa — a bit names.txt previously did not document at all), bit13 = dead, i.e. NOT a live rider on the playfield — set by a death (0x14098, 0x13ac8, 0x13e72) AND by the egg hatch (0x1277e), cleared when the rider is placed (0x1252a, 0x11f60), and tested at 12 sites. start_death_anim sets BOTH, hence its 0x3000, bit14 = bumped a platform edge, bit15 = facing right.
cmt 0x113ba pterodactyl_table: 4 slots x 0x20 bytes. Fields: +0 flags.w (bit0 = just spawned, bit2 = moving right, bit5 = dying), +2 dst.l, +6 src.l, +0xc x.w, +0xe y.w, +0x10 x speed.w, +0x12 y speed.w, +0x16 dst offset.w, +0x1a anim.w, +0x1c..0x1f direction/dwell timers.
cmt 0x117b4 platform_table: 8 records x 8 bytes {y0.w, y1.w, x0.w, x1.w} — the landable boxes, gated by platform_present[i] (0x10cfa, one byte per platform: 0 = absent this wave, >0 = needs a redraw, -1 = drawn).
cmt 0x117f4 platform_edge_table: 28 records x 12 bytes {y0.w, y1.w, x0.w, x1.w, y-push.b, x-push.b, platform index.w} — NOTE THE ORDER, the previous comment had the two push bytes the other way round: +8 drives the VERTICAL response (<0 snap to the box top +5, >0 push down 4) and +9 the HORIZONTAL one (<0 roll left, >0 roll right). No shipped record pushes both axes — the bump boxes used by render_object_body's bit14 branch to push an object off a platform edge and force a platform redraw.
cmt 0x119d4 platform_sprites: 8 records x 16 bytes {ptr to platform_present[i], rows.w, cols.w, src.l, dst offset.l} — the platform bitmaps, OR-blitted by draw_platforms and used as the pixel-collision surface by collision_check/test_overlap.
cmt 0x11964 spawn_points: 5 longs per record, terminated at 0x119b4 {in-use.b, y0.w, y1.w, x0.w, x1.w, y.w, x.w}. render_object_body walks them with spawn_point_cursor looking for one clear of every object before respawning a rider there.
cmt 0x10e16 message_table: records of 12 bytes {kind.b, timer.b, colour.b, shift.b, screen ptr.l, string ptr.l}, running up to object_table. find_free_message returns the first slot with kind==0 in a0.

# ==== comments: verified renames (evidence) ====
cmt 0x10a56 play_sound(index.w @4): if index <= snd_priority, store it and XBIOS Dosound(sound_table[index]) — trap #14 opcode 0x20 is Dosound (38/0x26 is Supexec). Lower index = higher priority. Was mis-named set_color_lvl from a wrong XBIOS table.
cmt 0x10a8a snd_poll_done: XBIOS Giaccess(0, 7) reads the YM2149 mixer; when all six tone+noise enables are off (0x3f) the sound has finished, so snd_priority is released to 0x10 (idle). Called once per frame from _start. Was mis-named read_key_flag.
cmt 0x1096e snd_tone_sweep: writes YM2149 registers 0,1,2,3,7,8,9,0xa via XBIOS Giaccess in a nested loop that steps snd_sweep_pitch down by 2 and snd_sweep_volume down by 1 — a descending two-channel sweep played at the end of init_video. Was mis-named init_gfx.
cmt 0x11d9a read_joysticks: Ikbdws(0, ikbd_cmd_joyread) sends the IKBD 'interrogate joysticks' command (0x16), spins until the IKBD handler stores the reply in ikbd_packet, then calls control_player with a0=player1/d0=joystick1 and a0=player2/d0=joystick0. Nothing here waits for vblank — was mis-named wait_vsync.
cmt 0x11dd6 control_player(a0=player object, d0.b=joystick byte): bit7 fire = flap, bit3 = right (vx +4), bit2 = left (vx -4). With no players left, fire restarts the game (game_over_flag, check_highscore, init_game, init_video and back into the main loop).
cmt 0x11c24 poll_quit_key: Bconstat/Bconin; only Ctrl-C (3) acts — Dosound(silence), write hiscore_name to HIGH.SCO (Fopen/Fcreate + Fwrite 0x1a bytes + Fclose) if hiscore_dirty, restore conterm/Setscreen/IKBD vectors/palette, Pterm. Every other key falls through the 0x11d58 table and returns.
cmt 0x105c2 rng_advance: the pseudo-random source is a pointer (rng_ptr) walking the program image 2 bytes at a time and wrapping at 0x17832; callers read words at *rng_ptr for randomness (spawn side, spawn x, explosion dither). On wrap it restarts at 0x10000 + (d0 & 0xfe), so the caller's d0 nudges the restart point by an even offset. Both constants are RELOCATED longwords — the raw listing shows #$7832 and #$0. The cmpi.l/blt compare is signed, and d0 is stacked across the wrap and restored, so callers keep it.
cmt 0x102fe make_fill_pattern(d0.b = colour): builds the 4-plane fill longwords d1:d2 tested by fill_screen's move.l d1/d2 loop. DEGENERATE, AND GENUINELY SO: all four btst/or blocks (0x10302/0x1030e/0x1031a/0x10326) are byte-identical (btst #0 / or.l #$ffff0000,d1), so the last three are no-ops, d2 is cleared and never written, and only bit 0 of the colour survives — into plane 0, making it a 2-colour fill. Not a depacker artifact (token trace) and not mid-entry aliases (nothing branches to 0x1030e/0x1031a/0x10326). All three fill_screen callers pass colour 0, so the game never notices. Reproduced as-is by recreate/src/fill.c.
cmt 0x10334 fill_pattern_n: unreferenced. Fills d0 groups of 8 bytes at (a0)+ with d1/d2 — a count-driven twin of fill_screen's loop. No caller anywhere in the image.
cmt 0x135f4 select_sprite_base(a0=object, d0.w=flags): returns in d1 the rider sprite-set base — 0x1c64a for player 1, 0x1ebaa for player 2, 0x22f4a otherwise — plus 0x130 when the facing bit (15) is set. Callers store d1 into draw_src. Was mis-named stub_ret2 (Ghidra decompiles it to `return;` because the result is a register).
cmt 0x1033e count_objects_and_pad: counts live objects (live_object_count) and eggs (egg_count) when game_phase==0, or message characters (message_char_count) otherwise — then burns busy-wait loops proportional to (8 - live), (0x0e - eggs) and (0x23 - chars). The delays are a frame-time equaliser so the frame costs the same however much is on screen.
cmt 0x1052e draw_platforms: OR-blits every platform_sprites record whose platform_present byte is > 0 (then latches it to -1), after forcing the paired platform flags 2/3 and 6/7 to agree. This is the repair pass that redraws platforms sprites have overdrawn. Was mis-named anim_sprites.
cmt 0x1757a raise_floor: every 7 frames (while the counter at 0x10d65 is non-zero) playfield_bottom moves up one 0xa0-byte row and paint_floor_row paints TWO STRIPS of it — cells 0-4 (x 0-79) and cells 15-19 (x 240-319), because the callee leaves a1 advanced 0x28 and the caller then adds 0x50. That is the exposed lava either side of the ground platform (x 64-255), overlapping it by one cell at each end. wave_manager adds 5 rows per wave for waves 0-3, so the floor/lava builds up over the opening waves; any object whose draw address reaches playfield_bottom is flagged 0x100 and dies. Was mis-named scroll_screen_up.
cmt 0x175ba paint_floor_row(a1 = row): for 5 groups of 4 plane words, sets plane 3 wherever all four planes are 0 — i.e. recolours background pixels to colour 8 without touching existing graphics. Was mis-named clear_sprite_mask.
cmt 0x17438 dissolve_platforms: for each effect_table record, looks up platform_sprites[kind] at 0x119c4 + sign_ext16(kind*0x10) — EFF_KIND is 1-BASED, so the base is one record BELOW platform_sprites (0x119c4, NOT 0x119c8), and the offset is sign-extended from 16 bits, un-draws the platform bitmap a row at a time and ORs random noise sampled through rng_ptr over it. wave_manager fills effect_table from (old layout mask AND NOT new), so this animates the platforms that vanish between waves. Was mis-named draw_explosions.
cmt 0x175de animate_ground_shrink: gated on the counter at 0x10d68 (set for waves 3-4). Walks a 0x10d68..0x10d9e state block, blits two sprites (0x18636 / 0x187e6) with blit_sprite_mask+blit_sprite, and on wave 3 steps ground_x0 up and ground_x1 down — platform 0 (the ground) narrows. CONFIRMED (# ctx discharged): 0x18636/0x187e6 render as LAVA FLAMES — four 18-row frames 0xd8 apart (0x18636..0x18996), colours 3/7/8/9, a flicker with a rising spark; 0x187e6 is frame 2, i.e. the right flame starts mid-cycle. One flame creeps right (pixel phase up, rolling into a cell step) and the other left, and on wave 3 ground_x0/ground_x1 close in one pixel per step behind them — so the ground narrows from BOTH ends with a flame at each retreating edge. Once the two are within 0x60 bytes the strip drops a scanline and loses a row; below 0x11 rows it climbs back WITHOUT refreshing the latch. Gotcha: the right flame rolls over on subq.w+bge (N==V, the sign of the TRUE difference, so phase 0x8000 rolls over though the stored 0x7fff looks positive); the LEFT flame is not symmetric — addq.w plus a SEPARATE cmpi.w, where the wrapped value decides. Was mis-named draw_scroller.
cmt 0x182a2 pos_to_screen(x@10.w, y@12.w -> dst@4.l, shift@8.w): dst = screen_base + y*0xa0 + (x/16)*8, shift = x mod 16. Callers push y, x, then a word and a long of space, and pop the results into draw_dst / draw_shift. Both offsets are folded in with adda.w, i.e. only the low word of each product, SIGN-EXTENDED — so from y=205 on (y*0xa0 > 0x7fff) the address runs backwards. Off-screen only, but the reconstruction matches it.
cmt 0x182d6 screen_to_pos(dst@4.l, shift@8.w -> x@10.w, y@12.w): the inverse of pos_to_screen — divides (dst - screen_base) by 0xa0 for y and by 8 for the word-group. Unreferenced anywhere in the image (dead code), and its x reconstruction multiplies the group by 22 rather than 16, so it would not round-trip. Was mis-named pos_to_screen2.
cmt 0x144b0 flash_hiscore_color: bumps a counter and XBIOS Setcolor(10, 0x400 | (n & 7)) — pen 10 flashes through 8 shades. Its only caller is the high-score entry wait loop.
cmt 0x144d4 hiscore_key_input: Bconstat/Bconin for the name entry — backspace moves the cursor left, RETURN finishes (jumping back into _start at 0x10006), space and A-Z (lower case folded up) store into draw_dst_off, redraw via draw_hiscore_entry and advance draw_shift (the cursor index, capped at 15). Was mis-named read_key.
cmt 0x14538 hiscore_joystick_input: same IKBD interrogate as read_joysticks, picks the stick belonging to the entering player (draw_src == player2), fire finishes the entry, up/down step the current letter through ' ' and 'A'..'Z' with wrap, left/right move the cursor; 0x1415e is the auto-repeat delay. Was mis-named vsync_wait2.
cmt 0x1437a check_highscore BUG: both comparison loops destroy their own counter — `move.b #$7,d0` sets a 7-character count, then `move.b (a0)+,d0` immediately OVERWRITES it with the character. The loop therefore exits only when the two strings differ, or when a character equals 1. Score bytes are never 1, so two IDENTICAL score strings scan past the end of both records until some later byte differs — an unbounded walk, not a 7-byte one. Same pattern at 0x143d2.
cmt 0x1437a check_highscore: runs only when game_over_flag is set. Picks the higher of the two players' score strings, and if it beats hiscore_name it marks hiscore_dirty, copies the score, clears the screen, and enters the name-entry loop (hiscore_key_input / hiscore_joystick_input / flash_hiscore_color) which never returns — RETURN or fire restarts the game.
cmt 0x146a6 draw_hiscore_entry: stores draw_dst_off (the current letter) into hiscore_name[draw_shift], sets text_shift 1 or 9 for the odd/even column, points text_ptr at screen_base+0x52b0 + cursor*4 and draws the single character.
cmt 0x14658 draw_hiscore_cursor: paints the 0xfefefefe underline row at screen_base+0x57b0 and then the wide cursor bar at the column given by draw_shift.
cmt 0x14160 score_update(a0=object) / 0x14166 score_update_p2 / 0x14172 score_update_p1: these do NOT take an amount — callers add directly into the ASCII score digits at object+0x41..+0x43, and this propagates the decimal carries up the string, awards an extra life (and draw_lives + play_sound) each time the 10000s digit turns even, then redraws the score. Was mis-named add_score*.
cmt 0x14246 draw_lives(a0=object): dispatches to draw_lives_p1/draw_lives_p2 by comparing a0 with player2, then draws five life glyphs (0x1861d / 0x18629) or blanks (0x1861b) from object+0x4c. Was mis-named bonus_life.
cmt 0x1435c find_free_message: returns in a0 the first message_table slot whose kind byte is 0 (the caller fills in string/timer/colour/position). It tests nothing else. Was mis-named check_messages.
cmt 0x12606 update_eggs: walks object_table and drives each object's egg sub-record (+0x1e state). States seen: 0x24 falling into the lava, 0x22 resting on a platform, 0x21 ready to hatch, 0x23 thrown, 0x20/0x19 the rider death animation, 0x12/0x11.. the hatch countdown, >0x20 in flight (update_egg_physics + gravity). Hatching turns the egg back into a rider with the buzzard sprite sets 0x18b9a/0x18aea/0x18c4a. Was mis-named animate_objects.
cmt 0x1285c update_egg_draw: the draw/advance tail of update_eggs, entered by jumps — erase_egg_sprite / draw_egg_sprite and then the next object. Ghidra split it into its own function because the loop jumps back here. Was mis-named draw_objects_buf2.
cmt 0x12914 draw_egg_sprite: ORs the egg sprite (draw_src, 4 words x draw_rows, stride 0x4d words) at draw_dst with draw_shift, plus the wrap-around column pass; if the destination reaches playfield_bottom the egg state becomes 0x24 (fell in the lava). Was mis-named draw_sprite_at.
cmt 0x129b4 erase_egg_sprite(a0=object): AND-NOTs the egg sprite recorded at object+0x2a/+0x2e/+0x32/+0x33 out of the screen, including the wrap-around column. It allocates nothing. Was mis-named alloc_object.
cmt 0x12a2a update_egg_physics(a0=object): tests the egg's box against platform_table (landing: state 0x22, egg sprite 0x1899a, roll direction from platform_edge_table) and then against platform_sprites; sets the roll/bounce sprites 0x18a0a / 0x18a7a and, once the egg has settled and stopped rolling, state 0x21 = ready to hatch. Was mis-named advance_anim.
cmt 0x12caa render_objects: entry — a0 = object_table, falls into render_object_body.
cmt 0x12cb2 render_objects_next: the loop tail (a0 += 0x4e, stop at effect_table). Not a callable routine — it is only ever branched to. Was mis-named render_objects_b.
cmt 0x12cc2 render_object_body: per-object physics + draw. Branches on the flags word: bit14 platform-edge bump, bit8 falling into the lava (death + score), bit7 respawn via spawn_points, bit9 walking on a platform, otherwise flight with gravity; then selects the rider sprite set (0x1a80a/0x1cd6a for the players, 0x1f2ca/0x201ea/0x2110a by enemy type, 0x2202a when dead) and erases + redraws through draw_object_mask/draw_object_data. Was named render_objects_impl.
cmt 0x13300 check_platform(a0=object, d0=flags): scans platform_table for a present platform containing (object x, y); on a hit it snaps y to the platform top and returns d0 | 0x200 (standing), otherwise it clears bit 9 and resets the walk state.
cmt 0x13628 flash_spawn_pad(a0=object, d0=flags): indexes the spawn point recorded in object+8 (offset into spawn_points), takes a 3-longword pattern from 0x1194c and a plane-select byte from 0x11944, and calls blit_pattern_rows three times — the effect drawn while a rider materialises. CONFIRMED (# ctx discharged) — and the NAME WAS WRONG: it is not a sparkle. 0x1194c renders as a SOLID TAPERING TRAPEZOID (30/26/22 px over 3 rows) painted in a FLAT colour cycled from spawn_pad_colors at 0x11944 (04 01 01 04 00 07 02 00 — two entries are colour 0, which blanks it); blit_pattern_rows either ORs the three row words into the selected plane or NOTs-and-ANDs them, i.e. a flat fill, never a highlight. Every platform bitmap already carries a colour-4 pad (30/28/24 px) baked into its top three rows, and for ALL FOUR spawn points this routine lands exactly on it: pad x = cells*16 + shift = 67 / 9 / 38 / 27, matching the first colour-4 pixel of platforms 0 / 2 / 4 / 5. Corroborated by the spawn record itself — +12 = x = pad_x + 6 and +10 = y = padrow - 1 for all four, and +18 points at that platform's platform_present byte. So it REPAINTS the platform's own spawn pad in a cycling flat colour while a rider materialises (the flash sits one pixel inside the baked pad on rows 1-2). Renamed draw_spawn_sparkle -> flash_spawn_pad; was mis-named blit_sprite3 before that.
cmt 0x1369c blit_pattern_rows(a3=dst, d2=plane select, d5/d6/d7=row data): for each of 4 plane words, ORs the data in where the d2 bit is set and AND-NOTs it out where it is clear, on three rows at strides 0 / 0xa0 / 0x140.
cmt 0x13842 collision_check: per object, box+pixel tests (test_overlap) against platform_sprites, then the other objects (joust: the higher rider wins — start_death_anim for the loser, score for the winner, joust_bounce otherwise), then the eggs held by other object slots (the 250/500/750/1000 chain from egg_bonus_table), then pterodactyl_table.
cmt 0x13ef2 test_overlap: broad phase for two boxes staged in hit_box_a/hit_box_b {dst.l, src.l, cols.w, shift.b, rows.b, cur col.w, y.w}; if their y bands overlap it aligns both to the same column and calls pixel_collision for the overlapping rows, setting collision_hit.
cmt 0x13fe6 pixel_collision: ORs the four plane words of each sprite row (plus the wrapped word when a shift is in play), shifts both to a common origin, and sets collision_hit = 0xff on the first row where the two masks share a bit.
cmt 0x14098 start_death_anim(a0=object): begins the dismount/death sequence — clears the egg-chain counter, points the egg record at the death sprite (0x1922a for player 1, 0x193da otherwise) one row above the rider, sets the egg state to 0x19/0x20, adds 5 to the TENS digit at +0x43, i.e. 50 points (the UNITS digit at +0x44 is held at '0' by the game, so every score is a multiple of ten) and returns flags | 0x3000 (dead + removed). It does not respawn anything; the respawn is render_object_body's bit7 branch. Was mis-named respawn_player.
cmt 0x140fe joust_bounce(a0, a3 = the two objects; d0/d1 = their flags): a draw. Compares the two x positions and flips both riders' horizontal velocity and facing bit so they bounce apart. The higher-rider-wins decision is in collision_check, not here.
cmt 0x14ada update_pterodactyl: the pterodactyl scheduler and mover. A free slot arms when spawn_timer runs out (spawn_interval halves each time), enters from the left or right depending on a bit sampled through rng_ptr, avoids platforms (ptero_avoid_platform), homes on the nearer player (ptero_spot_player), and posts the "1000" popup (0x152be) when it dies. wave_manager arms a slot immediately from wave 16 on. Was mis-named spawn_wave.
cmt 0x15226 ptero_avoid_platform(a0=pterodactyl): when the pterodactyl's x is between 0x3c and 0xfa, scans platform_sprites for a platform whose row band brackets its y and returns a vertical step of 4 (0 when clear) so it climbs or dives around the platform. Was mis-named check_platform_y.
cmt 0x15276 ptero_spot_player(a0=pterodactyl, a3=player, d0=flags): when the player is within +/-4..10 rows and 8..0x69 pixels ahead in the direction of travel, plays sound 3 and sets the pterodactyl's +0x1e swoop timer to 0x14. Was mis-named check_proximity.
cmt 0x146f6 lava_troll: from wave 4 on. Picks any object below y 0x8e that lingers over the pit, walks the hand (troll_x/troll_y) up toward it a frame at a time from the sprite table at 0x14aba, and on contact sets the object's bit4 (grabbed) and drags it down; releases if the object dies or respawns. troll_erase_hand undraws the previous frame (AND mask), troll_draw_hand ORs the new one.
cmt 0x149b8 troll_erase_hand: AND-masks the PREVIOUS frame's hand (troll_prev_src + 0x90, troll_prev_dst, troll_prev_shift, troll_prev_rows) out of the screen; returns early when nothing moved. Was mis-named troll_draw.
cmt 0x14a32 troll_draw_hand: when troll_state bit 0 is set, ORs the current hand sprite (draw_src, draw_dst, draw_shift, draw_rows) into the screen, clipped at playfield_bottom. Was mis-named troll_grab.
cmt 0x1783c wave_manager: drives game_phase. With game_phase==0 it declares the wave over once egg_count is 0 and every surviving object is a player, then hands out the team/survival/gladiator bonus messages. Counting down to 0 starts the next wave: bump wave_num and wave_num_text, raise the floor for waves 0-3, arm animate_ground_shrink for waves 3-4, load the platform layout mask from 0x11b58, seed effect_table with the platforms that disappear, recompute speed_type1/2/3, and spawn the new riders (flags 0x81/0x82/0x83) at free spawn points. Posts "WAVE"/"EGG WAVE"/"PTERODACTYL WAVE"/"GLADIATOR WAVE"/etc from 0x18429.
cmt 0x142de draw_messages: walks message_table, counts each slot's timer down (instantly when no players are left), draws it through draw_string, and when the "THY GAME IS OVER" string (0x185c5) expires sets game_over_flag.
cmt 0x10700 draw_string(string ptr @4): control bytes 1 = set x,y (two words, via pos_to_screen), 2 = colour, 3 = background colour (bit4 of text_flags), 8 = back up one character, 9 = select the 8-row font (0x17eba) instead of the 5-row font (0x18192), 0 = end.

# ==== blit helpers: semantic param names + register maps ====
# stack-arg blits: commit a signature with explicit stack storage -> named params
proto 0x1042a dst@4:4 src@8:4 cols@12:2 rows@14:2
proto 0x10456 dst@4:4 src@8:4 cols@12:2 rows@14:2
proto 0x10482 dst@4:4 src@8:4 cols@12:2 rows@14:2
# register/struct-glue blits: role map kept in a comment (params passed in regs/globals)
cmt 0x1042a blit_copy(dst@4, src@8, cols@12.w, rows@14.w): rect blit src->dst, dst advances 0xa0/row. cols/rows are counted down with subq.b, so only the LOW BYTE of each word is the count and 0 means 256 passes; src is read straight through, nothing clips, and rows wider than 0xa0 overlap.
cmt 0x10456 blit_or(dst@4, src@8, cols@12.w, rows@14.w): OR src onto dst, dst advances 0xa0/row. Byte-identical to blit_copy but for the combine opcode — same subq.b counts (0 = 256).
cmt 0x10482 blit_andnot(dst@4, src@8, cols@12.w, rows@14.w): AND-NOT (mask) src onto dst via not.l/and.l, dst advances 0xa0/row. Same subq.b counts (0 = 256).
cmt 0x17752 blit_sprite_mask: a5->{src=[0], dst_off=[1], shift=[2].w&0x3f, plane=[10].b}, d7=rows; AND-mask pass, dst=screen_base+dst_off, row stride 0x50 words.
cmt 0x177a2 blit_sprite: a5->{src=[0], dst_off=[1], shift=[2].w&0x3f, plane=[10].b}, d7=rows; AND mask + OR data, row stride 0x50 words.
cmt 0x136e8 draw_object_data(a0=object): src=draw_src, dst=draw_dst, shift=draw_shift, rows=draw_rows, half-select=draw_half_select; OR data, 4 words/row, stride 0x4d. Sets the object's bit8 (falling into the lava) if dst reaches playfield_bottom.
cmt 0x137bc draw_object_mask(a0=object): dst=[a0+0x14], src=[a0+0x18], rows=[a0+0x1c], shift=[a0+0x1d]; AND-NOT mask pass over the object's PREVIOUS position, stride 0x4d.
cmt 0x15098 blit_mask_wide(a0->{src=[6]+0x120, dst_base=[2], dst_off=[0x16], shift=[10].w&0x3f, rows=[0x18]}): AND-mask, 8 words/row, stride 0x50 — the pterodactyl's erase pass.
cmt 0x1510c blit_sprite_planes: src=draw_src, dst_off=draw_dst_off, shift=draw_shift, rows=draw_rows; per-plane enables at 0x10d0e/0x10d0f/0x10d10 clip the sprite at the screen edges; OR data, stride 0x50 — the pterodactyl's draw pass.
cmt 0x10c46 xbios_setpalette: XBIOS Setpalette(palette) — the 16-entry table at 0x10cd2.
cmt 0x10c56 cycle_palette: rotates the single hue held in palette entry 4 (0x10cda) through R/G/B, selected by bits 8-10 of palette_cycle_ctr.

# ==== comments refined during the reconstruction (verified against the oracle) ====
# These correct or deepen earlier cmt lines above; where they disagree, these win —
# each was checked instruction-by-instruction while porting the function to C.

cmt 0x10d0e draw_clip_cell0/1/2: per-CELL suppressors blit_sprite_planes tests once per row (tst.b + bne). NONZERO means SKIP that cell — leading (dst+0), middle (dst+8), trailing (dst+16). Staged by update_pterodactyl (0x1502a) to clip the sprite at the screen edges. NOT "per-plane enables".
cmt 0x17eba font_large_glyphs: 8 bytes per character from ' ' up, 6 px wide. Selected by bit 7 of text_flags.
cmt 0x18192 font_small_glyphs: 5 bytes per character from ' ' up, one byte per row, 4 px wide. draw_string indexes it as (ch - 0x20) * 5; chars below ' ' index BACKWARDS out of the table and are still rendered.
cmt 0x11a54 egg_sprite_ptrs: a JUMP TABLE, not a sprite table — 8-byte records {sprite.l, handler.l} indexed by (egg state - 1), read at 0x12726/0x1272e and entered with `jmp (a2)`. Only two handlers exist in the reachable range: 0x12854 (draw only, then the next object) and 0x128c0 (update_egg_draw's erase/draw/commit tail). Records 3, 24 and 31 (states 4, 0x19, 0x20) are ALL ZERO and would `jmp 0`; none is reachable — state 5 hatches rather than stepping down to 4, and the animation clears 0x19/0x20 instead of dispatching them. The index is (state-1) via subq.b and is never bounds-checked, so a state >= 0x80 indexes past the table entirely.

cmt 0x102e2 fill_screen(colour.w @4): 4001 cells of 8 bytes from screen_base = 32008 bytes — EIGHT PAST the 32000-byte framebuffer, every call. Passes the colour to make_fill_pattern, which is degenerate, so only bit 0 survives.
cmt 0x136e8 draw_object_data (append): re-reads playfield_bottom EVERY row and, on the lava exit, computes the rows-drawn byte as a FRESH read of draw_rows minus the row counter (sub.b/neg.b) — so a sprite drawn over either global bends the pass. Stopping on row 0 leaves draw_rows = 0, which the wrap column's subq.b loop reads as 256 rows.
cmt 0x1510c blit_sprite_planes (append): three passes per row in a fixed order — middle cell, leading cell, then the trailing cell, whose four source words are RE-READ from memory after the first two passes have written. Each suppressor is tested once per row.
cmt 0x17752 blit_sprite_mask (correct): a5->{src=[0], dst_off=[4].l, shift=[8].w, cell_select=[0xa] LOW BYTE, signed}. blt skips the trailing cell and bgt the leading one, so cell_select < 0 draws the LEADING cell alone, > 0 the TRAILING one, 0 both. Mask at src+0x90, 4 bytes/row.
cmt 0x177a2 blit_sprite (correct): as blit_sprite_mask plus the colour data ORed in behind the mask — four plane words at src, 8 bytes/row, SHIFTED (not rotated). Same cell_select polarity.

cmt 0x13fe6 pixel_collision(A1=box_a, A2=box_b): walks hit_rows rows of two sprites already aligned on a column by test_overlap; ORs each cell's 4 plane words and stops at the first shared bit. When cur_col != 0 AND shift != 0 the previous cell is swapped into the high half so a shifted sprite's spill is seen. On a hit: collision_hit = 0xff and box_a.cur_col = cols-1, which ends test_overlap's column sweep. Row stride is adda.w — low word only, sign-extended.
cmt 0x13ef2 test_overlap: broad phase over hit_box_a/hit_box_b; exg puts the lower screen address in a1. Band test uses add.b, so y's low byte wraps instead of carrying. Column offset = (dst gap divu.w #$a0) remainder / 8; if that lands outside the upper sprite it is re-measured backwards (20 - n) against the lower one. MUTATES both boxes' src and cur_col as cursors.
cmt 0x140fe joust_bounce(A0=obj_a, A3=obj_b, D0/D1=flags): height tie — both riders turned away and pushed apart. "A is on the left" iff gap >= 0xffee unsigned OR gap >= 0x12e signed — x wraps at 320, so a gap of -19 or less really means A is on the right. Velocity written only on a flip.
cmt 0x13300 check_platform(A0=object, D0=flags) -> D0: scans platform_table for a present box holding (x,y); snaps object y to the platform top (+1 at flap frame 4) and sets bit 9. Off every platform: clears bit 9 and, only if it HAD been set, restarts the walk (anim 3, step 5, vy 0).
cmt 0x129b4 erase_egg_sprite(A0=object): NOT an allocator despite the old alloc_object name. AND-NOTs the egg out in two passes over the same source rows: main, then the bits that spilled past the cell — one cell right, or a scanline BACK once egg x >= 0x130. Source row stride 0x10, of which only the first 4 words are read. Row count is subq.b: 0 means 256.
cmt 0x12914 draw_egg_sprite(A0=object): ORs the egg in from the draw_* globals; returns at once if the slot carries no egg. If dst reaches playfield_bottom the egg is in the lava: state = 0x24 and draw_rows is rewritten to the rows actually drawn. A hit on row 0 leaves draw_rows = 0, which subq.b reads as 256 — the wrap pass then marches 256 scanlines down. Original behaviour, reproduced.
cmt 0x15226 ptero_avoid_platform(A0=ptero, D1=scratch) -> D1: returns 4 while a platform's row band, extended 13 rows upward, brackets the pterodactyl's y; 0 when clear or outside x 0x3c..0xfa. ONLY D1's low word is written, so the high half returns the caller's value on the clear exits and the matching record's divu.w remainder on a hit. Every caller reads move.w d1.
cmt 0x15276 ptero_spot_player(A0=ptero, A3=player, D0=flags): arms the swoop (timer 0x14, sound 3) when the player is within -4..+6 rows and in front. The +8 x bias is applied BEFORE the direction flip, making the window asymmetric: 1..0x60 left when flying left, 0x11..0x70 right when flying right. The cmpi.w #$ff29 test is strictly redundant — the later signed ble #$8 already rejects every value it does. Kept as shipped.
cmt 0x1033e count_objects_and_pad: per-frame census plus a frame-time equaliser — each count is followed by a busy-wait proportional to the work NOT done: (8-live), (0x0e-eggs), (0x23-chars). Those loops touch no memory. Between waves (game_phase != 0) it counts message characters instead, and QUIRK: the walk follows the message's SCREEN pointer (+4), not its string pointer, so it counts screen bytes to the first zero. Only a delay loop reads it.

# ==== comments: per-frame core (verified against the oracle) ====

cmt 0x14172 score_update / _p1 / _p2: three entries 6 bytes apart falling through into one body at 0x1417c; the two aliases load player 2 / player 1 into a0 and IGNORE the a0 they were called with. They add NOTHING. (1) Promotion sweep over +0x3e..+0x43: skip leading spaces, then lift any byte below '0' (a SIGNED compare) by 0x10 — that is what turns a caller's `addq.b #5` on a blank column into a digit. It stops one short of +0x44, safe only because the units digit is held at '0'. (2) Carry sweep from +0x44 down: while a digit is above '9' (UNSIGNED), promote the column to its left from ' ' to '0', bump it, take 10 off here; a carry OUT of +0x41 leaving +0x40 EVEN pays an extra life (+1 at +0x4c, draw_lives, play_sound(1)) — one per 20,000. (3) bset #7/#4 text_flags, bg colour 0xf, text_shift from +0x3b, text_ptr from +0x36, draw_string(a0+0x3c), bclr #4. BUG: the leftmost digit's carry lands on +0x3d, the score string's own colour byte, so an eight-digit score recolours its HUD row.
cmt 0x146f6 lava_troll: from wave 4 on (SIGNED byte compare). troll_step_timer is a subq.b+bge clock reloaded to 2, so the hand animates every third call. The scan takes the first slot alive, not respawning/dead/in-lava, at or below y 0x8f, with x-0x32 ABOVE 0xdc unsigned (x < 0x32 or x > 0x10e — the two ends of the lava). With the hand out the object must also be within 12 px right, tested TWICE: unsigned <= 0xc and signed <= -0x134, the same window measured the other way round the 320-px screen. A raised hand takes prev_src = 0x18f0a (a RELOCATED immediate) and prev_dst = 0x5dc0 (a screen offset, NOT relocated). Contact is troll_y - obj_y in 0..0xb unsigned; the grab plays sound 6 and falls straight into the hold block, so the first carrying frame happens in the same call. A PLAYER climbing above y 0x8c is released and pays 5 into +0x43. BUG: the respawn/in-lava drop's `clr.w troll_state` at 0x48a2/0x48ba is DEAD — the tail's `move.w d0,troll_state` overwrites it — so the hand stays flagged out and the NEXT call retracts it; the following bclr/move.w re-stores the word already written. troll_sprite_table holds only FOUR records before update_pterodactyl's code.
cmt 0x12606 update_eggs: walks object_table driving each slot's egg sub-record (+0x1e state), staging the draw_* scratch from it first. 0x24 = sinking into the lava; 0x21 = settled and hatchable (waits for live_object_count < 8 SIGNED, then the +0x1f timer); >0x21 = in flight (update_egg_physics, gravity every +0x29 frames capped at dy=4, one move with the 320-px x wrap and a row-0 clamp that reverses a rising dy); 0x0b = the frame drawn 4 rows higher; 0x05 = the hatch (every rider hatches at speed_type1 whatever its type); anything else below 0x21 steps the animation down and dispatches through egg_sprite_ptrs. Its slot advance at 0x12612 is THE SUBSYSTEM'S ONLY rts, and seven branches leave by jumping into update_egg_draw rather than falling through.
cmt 0x1285c update_egg_draw(a0=object): pos_to_screen -> draw_dst/draw_shift, then — unless the record already names the same address, source, height and shift — erase the previous sprite (or, if +0x34 bit7 says it was never drawn, just clear that bit) and draw the new one, committing six fields into the egg sub-record. Ends `jmp 0x12612`, back into update_eggs' slot advance; 0x128c0 is a second entry used by the lava, hatch and jump-table paths. The matching-record shortcut is almost invisible — an AND-NOT then OR of the same bits equals the OR alone — and its only observable effect is that it also skips the never-drawn bit handling.
cmt 0x12a2a update_egg_physics(a0=object): three passes — platform_table landing, platform_sprites pixel collision via test_overlap, then platform_edge_table push. THAT LAST BRANCH DOES NOT RETURN: `adda.w #$4,a7 ; bra.w 0x1285c` discards its own bsr return address and tail-jumps into update_egg_draw, which returns to update_eggs' loop on its behalf, so A7 balances and the subsystem still has exactly one rts. Falling off the end of the edge table rts's normally with state 0x22 already written.
cmt 0x11dd6 control_player(a0=object, d0.b=joystick): LIVE RIDER — bit7 fire sets/clears BOTH flap bits (6 and 11); target vx = own vx, 0 while respawn is pending, +/-4 on the stick with right winning. DEAD RIDER (bit13) — leaves at x >= 0x13c facing right, or in the asymmetric 4-px window 0x130..0x133 facing left, then draw_object_mask + player_death; otherwise hovers, target y = 0x8c/0x42/0xa by band, flap pair TOGGLED while y > target and vy >= 0, else cleared. Every x/y test is a signed cmpi.w. EMPTY SLOT — fire with players_alive == 0 restarts and NEVER RETURNS: addq.w #8,a7 drops its own return address AND read_joysticks', then game_over_flag, check_highscore, init_game, the 0x11e50 tail, init_video, jmp 0x10018.
cmt 0x11e50 control_player's restart tail (an inner label, not a function entry): two_player_mode picks players_alive = 2 with player2 given score digit '0' and 4 lives, or players_alive = 1 with player2's flags cleared; player 1 always gets the same digit and 4 lives.
cmt 0x11f28 player_death(a0=object): `cmpa.l #player2 ; bls` is an UNSIGNED WHOLE-POINTER bound, so every slot at or below player2 loses a life and only the enemy slots above are cleared. subq.b on +0x4c, then the count is RE-READ and compared with 0xff, so only a count already 0 ends the turn. Otherwise clear prev_dst/prev_rows/vx and bits 4/13/8, set bit 7, repaint BOTH lives rows. End of turn: flags = 0, subq.b players_alive, and a banner into find_free_message's slot — str_game_over only on an exact 1->0, else str_player1_over/str_player2_over by a full cmpa.l. BUG: with all 24 message slots taken find_free_message returns 0 and the 12-byte record is written over addresses 0..0xb, the bottom of the vector page.

# ==== comments: the per-frame giants (verified against the oracle) ====

cmt 0x12014 update_objects(): SIZE CORRECTION — the routine spans 0x12014..0x12605 = 1522 bytes; decomp.c's inventory says 1512, which ends INSIDE the neg.w at 0x125fa. Once per frame it walks object_table slots 2..13 (enemy_objects..effect_table) and sets each live enemy's steering targets — target_y (+0x46) and target_vx (+0xc) — plus its flap bits; render_object_body then eases the real velocity toward them. The two PLAYER slots are never visited (control_player steers those). Dispatch on the flags word's low 2 bits: 0 = a player, stored back untouched. Eight shared tails do the flap bookkeeping. A NINTH at 0x12546 is DEAD — no branch in the image targets it and 0x12544 is a bra. Reads TWO registers before writing them: D0 (the flags word, handed to rng_advance as its wrap offset, so the FIRST slot sees the caller's D0 and each later slot the previous slot's stored flags) and D2 (a scratch word the type-1 player-2 chase test reads on the active_players==2 path that never sets it up). Its dead-rider exit strip + hover ladder at 0x1243e..0x1249c is instruction-for-instruction identical to control_player's at 0x11ea0.
cmt 0x1783c wave_manager: TWO NON-CONTIGUOUS CHUNKS — the dispatcher at 0x1783c..0x17e79 (1598 B) and the wave-start body at 0x17012..0x17437 (1062 B), reached ONLY by the beq.w at 0x178a0 and ending in its own rts. 1598+1062 = the 2660 the inventory counts; a linear sweep hides the split. The 64 bytes at 0x17e7a are NOT padding — they are the egg placement's scratch list. A state machine on game_phase, which doubles as a message generation tag. It also SCATTERS EGGS, which the old comment omitted entirely: when egg_wave_countdown is 0, 0x17228 places eggs on random platforms instead of lining up riders, and it does so by spending the SAME three type counts the rider loops read, leaving them at -1 — that is the whole mechanism by which an egg wave has no riders. The gladiator bounty is only ANNOUNCED, never paid. Type 2's facing test is INVERTED (bne where types 1 and 3 use beq), so type-2 riders face in antiphase. The survival banner is recoloured AFTER score_update returns — legal only because score_update saves and restores a0.
cmt 0x12caa render_objects / 0x12cb2 render_objects_next / 0x12cc2 render_object_body: ONE routine with three entries. render_objects loads a0 = object_table and falls in; it is the only entry the game uses (jsr at 0x10042). render_objects_next is the loop tail (a0 += 0x4e, rts at effect_table) and owns the family's SINGLE rts — nothing calls it, everything branches to it, so entering the body with a0 mid-table renders that slot AND every slot above it. TWO RESTART EDGES re-dispatch the same slot in the same call: 0x1319c (an edge bump that found no box) and 0x135f0 (a respawn finishing). During a respawn +6 (the HIGH byte of vx) doubles as the materialise frame counter and +8 (vy, as a word) as the byte offset of the spawn_points record. cmpa.l #enemy_objects,a0 at 0x1320c is DEAD CODE — no branch consumes its condition codes, so a player leaves a platform exactly as an enemy does.
cmt 0x12f96 the dead-rider edge clip: for a corpse (bit13) NOT flagged bit5 whose x is past 0x12f, one of draw_object_data's two passes is suppressed. btst #12 picks between two EXACTLY OPPOSITE mappings — bit12 clear keeps the leading cell while drifting left and the wrap column while drifting right; bit12 SET does the reverse. One of only two readers of bit 12 in the program.
cmt 0x13842 collision_check: one pass over object_table; each live, non-respawning slot runs four box+pixel sweeps. (1) platform_sprites — first overlap sets flags bit14 and, for a PLAYER only, plays sound 7; the btst #13 sits AFTER this sweep, so a corpse still scrapes platforms. (2) the other riders, starting one slot LATER so each unordered pair meets exactly once per frame: equal y ties, else the smaller y wins, and the player test is on the OBJECT not the winner. (3) the eggs held by slots 2..13, players only. (4) pterodactyl_table, players only and only while bit13 is clear. movea.l (a7),a0 at 0x13c76 is a DEAD LOAD of collision_check's own return address — find_free_message overwrites a0 before anything reads it.
cmt 0x13b60 the dismount's spawn flags (object+0x34): the unseated enemy's own type bits, bumped by one unless already 3, then bset #7. The low bits are the rider type the hatched rider inherits — so each generation comes back tougher — and bit 7 is the "never drawn" flag update_egg_draw reads to skip the first erase.
cmt 0x13e6a the pterodactyl death: a COPY of start_death_anim's body (0x14098) MINUS its clr.b 53(a0). So a player killed by a bird KEEPS its consecutive-egg chain, where a player unseated in a joust loses it.
cmt 0x17228 wave_manager's egg scatter: picks a platform (rng word AND 0x38, a whole platform_table record) and an x (a second rng word reduced modulo the box width, taking the divu.w REMAINDER via swap), re-rolling while the x repeats the last one or lands within +/-8 of an egg already there. Accepted pairs are kept at egg_spread_scratch. The type comes from wave_type1/2/3_count via subq.b + bge (N==V, so 0x80 reads NEGATIVE), leaving all three counts at -1 or below — which is what stands the rider loops down.
cmt 0x11b58 wave_layout_table: 0x33 longwords, one per wave, {platform mask.b, type1 count.b, type2 count.b, type3 count.b}, loaded WHOLE into wave_layout_mask. Indexed by wave_num sign-extended and x4, so a wave past 0x7f reads BELOW the table. It ends exactly at poll_quit_key (0x11c24), which is why the wave count wraps at 0x33.
cmt 0x10d13 respawn_lock: set when a rider of type <= 3 takes a spawn point (0x13422), cleared when a NON-PLAYER finishes materialising (0x134fe) and again at 0x1720e. An enemy will not start looking for a spawn point while it is set, so only one enemy grows out of a pad at a time; a player ignores it.
cmt 0x10d4e snd_owner: the object whose looping sound is playing, compared against the current slot before releasing it — so a rider only silences its OWN sound. Claimed AFTER the fact, because play_sound may drop a request a higher-priority sound holds.
cmt 0x10d5e hunter_counts: two ADJACENT bytes, one per player, counting the type-2 riders holding a chase slot on it. Compared against each other SIGNED and against the cap UNSIGNED; with no players left BOTH are zeroed by a single clr.w word store.
